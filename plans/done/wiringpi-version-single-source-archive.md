# Archive: wiringpi-version-single-source plan

Completed work from `plans/wiringpi-version-single-source.md`. Entries are moved
here by the maintenance rules in that plan; never renumber.

## Archived V Tasks

- V1: Write `scripts/audit-family-buildcheck.pl` — read-only family build-guard reporter — ✅ 2026-07-08 attempt 1: PASS (script derives the family from this repo's PREREQ_PM (RPi::* + WiringPi::API), maps module->slug with an OLED exception, locates each Makefile.PL local-clone-first with a raw.githubusercontent.com fallback via HTTP::Tiny (master then main), and reports per dist: guard class (wiringpi / i2c / both / none — via wiringPi.h vs i2c.h headers + version-check signals), the enforced wiringPi minimum, XS-vs-pure-perl, and drift vs canonical 3.18; `--markdown` emits a table, plus a status summary. Fixed a `scalar glob` iterator bug in the XS detector (switched to list-context glob) that had mis-flagged rpi-i2c as pure-perl. Ran clean over all 23 family dists — every one found locally, zero fetch errors. Findings: **2 canonical** (wiringpi-api + rpi-oled-ssd1306, both now 3.18 via RPi::Const — the old 2.36 drift was already fixed by Fix 1); **1 i2c-only** (rpi-gpioexpander-mcp23017); **1 wiringPi presence-only, no version check** (rpi-dht11); **8 GUARDLESS XS** that FAIL-not-NA on testers (rpi-adc-ads, rpi-dac-mcp4922, rpi-eeprom-at24c32, rpi-hcsr04, rpi-i2c, rpi-rtc-ds3231, rpi-serial, rpi-sysinfo); **11 pure-perl** needing no guard (incl. the 3 newly-added dists rpi-accelerometer-adxl335 / rpi-gyro-mpu6050 / rpi-radar-rcwl0516). No commit.)

- V2: Classify every family dist by which build check it needs (wiringpi / i2c / both / none) from what its XS actually links/includes — ✅ 2026-07-08 attempt 1: PASS (classified all 23 dists from Makefile.PL LIBS + `.xs` #includes; table + worklist appended to the plan's "Current known state" as "V2 classification". Needs: **5 wiringpi** (wiringpi-api, oled, dac-mcp4922, hcsr04, dht11 — all link `-lwiringPi`), **5 i2c** (gpioexpander, adc-ads, rtc-ds3231, eeprom-at24c32, i2c — raw `linux/i2c-dev.h`), **13 none** (rpi-serial + rpi-sysinfo are XS but pure-POSIX/standard-C so compile anywhere → guard unneeded; + 11 pure-perl). No dist needs "both". Refined V1: of its 8 guardless-XS, only **6 genuinely need a guard** (dac-mcp4922/hcsr04 wiringpi; adc-ads/rtc-ds3231/eeprom-at24c32/i2c i2c) — serial/sysinfo are false alarms. Identified the F1 2.36 paste as **rpi-oled-ssd1306** (already fixed to 3.18 by Fix 1). Fixed the stale "20 dists"/"only 4 local" facts: it's 23 dists, all local, so V7's local / V8's remote split collapses — V8 clones nothing. No commit.)

- V3: Implement `RPi::Const::BuildCheck` in rpi-const — ✅ 2026-07-08 attempt 1: PASS (new pure-perl module `lib/RPi/Const/BuildCheck.pm` with `wiringpi_build_check()` + `i2c_build_check()` — both honour `RPI_DIST_RELEASE` and do the exit-0 NA-not-FAIL dance before WriteMakefile. `$MIN_WIRINGPI_VERSION` is READ from `RPi::Const::WIRINGPI_MIN_VERSION` (absorbs Fix 1's constant — single source, literal 3.18 only as a can't-load fallback). Fixed F2 with `version_ge()`, an integer (major,minor) tuple compare (3.8 correctly fails a 3.18 minimum), and F3 by making an unparseable/absent `gpio -v` exit NA instead of silently passing. All I/O is injectable (`include_dirs`/`gpio_output`/`gpio_path`/`min_version`/`env_release`/`na`) so it's fully testable off-Pi. `t/55-buildcheck.t`: 34 subtests — tuple compare incl. 3.8/2.36/3.17 failing + 3.18/3.20/4.0 passing, `_parse_gpio_version`, the bypass, each NA path (missing header, too-old, unparseable, no-gpio), i2c present/missing, and a fork test proving the default NA exits 0. Full `make test` PASS plain and under RELEASE_TESTING (manifest.t + pod-coverage.t green). POD + Changes added; MANIFEST updated. No commit.)

- V4: Reference conversion of wiringpi-api/Makefile.PL to the BuildCheck shim — ✅ 2026-07-08 attempt 1: PASS (replaced the ~44-line inline wiringPi presence/version guard with a ~12-line shim: `eval { require RPi::Const::BuildCheck; ...wiringpi_build_check(); 1 } or do { print + exit 0 unless RPI_DIST_RELEASE }`; added `CONFIGURE_REQUIRES => { 'RPi::Const' => 1.07 }`; dropped `use version`. Verified on the Pi (has wiringPi 3.18, so the expectation differs from the plan's mac-no-wiringPi note): (A) normal run with BuildCheck on PERL5LIB -> check passes, Makefile written, exit 0; (B) RPI_DIST_RELEASE=1 -> bypass, Makefile written; (C) bare `perl Makefile.PL` with BuildCheck NOT installed -> NA-safe "install RPi::Const first" message, exit 0, no Makefile. `perl -c` clean. This is the template V6/V7/V8 copy (wiringpi shim); the i2c dists use `i2c_build_check()` instead. Changes entry added. No commit; final on-Pi FAIL-path (too-old/absent wiringPi) is Steve's since this Pi can't simulate a missing library, but that path is unit-tested in V3.)

- V5: Author-side drift gate `xt/author/buildcheck-audit.t` — ✅ 2026-07-08 attempt 1: PASS (wraps the audit script via `require` -> `audit_family()`. To make it robust I enhanced `scripts/audit-family-buildcheck.pl`: added a `caller`-guarded `_main`/`audit_family()`, per-dist `needs` (from LIBS + `.xs`/`.c`/`.h` #includes) and `shim` (Makefile.PL references RPi::Const::BuildCheck) fields, and made `drift_status` recognise converted dists. The gate asserts: (1) every dist reporting a concrete wiringPi minimum reports the canonical 3.18; (2) the set of needs-a-guard-but-not-yet-shim dists exactly equals the explicit TODO list (9 dists — oled, dac-mcp4922, hcsr04, dht11, adc-ads, rtc-ds3231, eeprom-at24c32, i2c, gpioexpander), so it shrinks as V7/V8 land; (3) every needs-a-guard dist NOT in TODO is shim-converted (wiringpi-api). Passes today (6 subtests). Demonstrated the drift catch two ways: an in-test synthetic 2.36 guard, AND a live edit injecting `version->parse("2.36")` into rpi-hcsr04's real Makefile.PL -> gate FAILS on "minimum '2.36' is canonical (3.18)", then restored -> PASS, clone left clean. Found + logged a real defect during the work (B5: vestigial `-lwiringPi` in pure-perl bmp180/digipot Makefile.PLs) and fixed `detect_needs` to ignore LIBS for non-XS dists. No commit.)

- V6: Docs — name `RPi::Const::BuildCheck` / `WIRINGPI_MIN_VERSION` as the canonical minimum across prose — ✅ 2026-07-08 attempt 1: PASS (three POD edits: (1) `WiringPi.pm`'s v3.18 prose (~line 717) now says the minimum is the family-wide constant `RPi::Const/WIRINGPI_MIN_VERSION` (currently 3.18), enforced at build time by `RPi::Const::BuildCheck` — "defined in exactly one place"; (2) `FAQ.pod`'s `RPI_DIST_RELEASE` section now names the canonical `wiringpi_build_check()` / `i2c_build_check()` guards it bypasses and cross-links the new entry; (3) new `FAQ.pod` `=head2 Where is the required wiringPi version defined?` under `=head1 DEVELOPMENT`, explaining the single-source constant, `CONFIGURE_REQUIRES`-pulled build check, the NA-not-FAIL behaviour, the one-release bump path, and the audit script + author drift gate. Careful NOT to touch the unrelated "As of WiringPi::API 3.18 the callback must be a code reference" references (a different 3.18 — the callback API change). Regenerated derived docs with `scripts/gen-pod-md.pl` (7 files + README.md); verified `grep -rn 'BuildCheck' docs/pod/FAQ.md README.md` shows the single source in both the source POD and generated md, no stale standalone version claims. Changes entry added. No commit.)

- V7: Convert the remaining local clones to the RPi::Const::BuildCheck i2c shim — ✅ 2026-07-08 attempt 1: PASS (converted both `rpi-gpioexpander-mcp23017` (replaced its inline `/usr/include/linux/i2c.h` presence check) and `rpi-i2c` (which had NO guard at all — added one) to `RPi::Const::BuildCheck::i2c_build_check()`, mirroring V4's wiringpi-api shim but calling the i2c check; added `RPi::Const => 1.07` to each `CONFIGURE_REQUIRES`. Kept rpi-i2c's friendly non-Linux `warn`+exit as a clearer message before the shim. Dependency hygiene: gpioexpander already uses RPi::Const at runtime so its `PREREQ_PM` RPi::Const was bumped 1.06->1.07; rpi-i2c does NOT use it at runtime (grep-verified), so it went into CONFIGURE_REQUIRES only, `PREREQ_PM` left empty. Verified on the Pi (i2c headers present): normal run -> check passes, Makefile written, exit 0; `RPI_DIST_RELEASE=1` -> bypass, Makefile written; bare `perl Makefile.PL` with BuildCheck NOT installed -> NA-safe "install RPi::Const first" message, exit 0, no Makefile (all three, both dists). Removed the two now-converted slugs from `xt/author/buildcheck-audit.t`'s %TODO; the drift gate now passes with 8 subtests (tests 5+6 assert each is shim-converted). Audit summary: 3 converted (wiringpi-api + these two), 0 drift. Per-dist Changes entries added. Discovery: `rpi-gpioexpander-mcp23017/Changes` has a stray committed `<<<<<<< HEAD` conflict marker at line 7 (no matching =======/>>>>>>>; coherent entries both sides) — flagged to the user, left untouched; logged as B6. No commit.)

- V8: Convert the remaining guardless XS dists that genuinely need a guard; finish the single-source migration — ✅ 2026-07-08 attempt 1: PASS (converted SIX dists to the shim: **wiringpi** — `rpi-oled-ssd1306` (replaced its inline `use version` / gpio-v parse block; its "i2c" includes are `<wiringPiI2C.h>` which ships with wiringPi, so it is pure-wiringpi, NOT i2c), `rpi-dac-mcp4922` (no prior guard; bumped RPi::Const 1.05->1.07), `rpi-hcsr04` (no prior guard), `rpi-dht11` (upgraded a presence-only check to the version-aware shim); **i2c** — `rpi-adc-ads` (`<linux/i2c-dev.h>`, no prior guard) and `rpi-rtc-ds3231` (`<linux/i2c.h>`+`<linux/i2c-dev.h>`, no prior guard). CONFIGURE_REQUIRES RPi::Const 1.07 added to all six; runtime PREREQ_PM only where actually used (oled, dac). **Key discovery — `rpi-eeprom-at24c32` needs NO guard**: it `#include "i2c-dev.h"` (quoted) — a header it BUNDLES (in MANIFEST) that pulls only `<linux/types.h>`/`<sys/ioctl.h>`; the .xs/.c use only standard headers, so it compiles without libi2c-dev and would be wrongly NA'd by an i2c guard. This corrects V2's classification (it had counted eeprom among the 5 i2c dists). Refined `audit-family-buildcheck.pl`: `detect_needs` now counts only ANGLE-bracket `<linux/i2c*.h>` as a real dep (a bundled quoted include is not), and `drift_status` reports a needs=none XS as "standard-C XS (no guard needed)" rather than a false "GUARDLESS XS" alarm — so eeprom/serial/sysinfo stop looking like gaps. Reworked the drift gate: emptied `%TODO` (migration complete), flipped the obsolete "at least one dist reports a min" test into "the minimum is fully single-sourced - no dist carries an inline min" (post-V8 no dist restates the literal; test 1 stays a regression guard and the synthetic-2.36 test still proves detection bites), and added a cross-check that the audit's CANONICAL matches `RPi::Const::WIRINGPI_MIN_VERSION` from the sibling checkout (graceful SKIP if absent). Gate green at 13 subtests. Audit: **9 converted, 0 drift, 0 genuine guardless**. Verified all three configure paths on the Pi for all six dists (normal->written, RPI_DIST_RELEASE=1->written, no-BuildCheck->NA-safe exit 0, no Makefile). Per-dist Changes entries added. No commit. The whole plan (V1-V8) is now functionally complete; remaining work is optional backlog B1-B6 and Steve's release sequencing (rpi-const first, then the converted dists).)

## Archived Fixes

_None yet._
