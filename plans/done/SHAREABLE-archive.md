# Archive: shareable-refactor

Completed V tasks and resolved fixes for `shareable-refactor.md`.

## Archived V Tasks

- V1: Swap dependency in `Makefile.PL` (`IPC::Shareable` >= 1.17 + `String::CRC32`, keep `JSON::XS`, drop `IPC::ShareLite`) — ✅ 2026-06-06 attempt 1: PASS
- V2: Rewrite `Meta.pm` backend to tie-a-SCALAR (IPC::Shareable + JSON::XS string + String::CRC32 key derivation); `meta`/`meta_key`/`meta_fetch`/`meta_store`/`meta_key_check` reimplemented, `meta_set/get/delete/erase` left intact — ✅ 2026-06-06 attempt 1: PASS
- V3: No-XS functional gate `build_testing/meta_shareable_check.pl` (16 assertions: key/key_check, lock/fetch/store, detachment, set/get/delete, erase 0/1, fork cross-process, single-segment no-fan-out); empirically confirmed scalar=1 segment vs hash=6; persistence across reruns verified via __runs marker — ✅ 2026-06-06 attempt 1: PASS
- V4: Update `t/02-shm_key.t` to expect `meta_key == 1473559184` (crc32 of 'rpit'); key_check rpit=1/blah=0 retained; 3/3 pass on the Pi (env: `PI_BOARD=1 RPI_OBJECT_COUNT=0`) — ✅ 2026-06-06 attempt 1: PASS
- V5: Meta-data tests on hardware (t/03, t/05, t/110 green; t/111 expected-skip without RPI_MULTI). Initial 3 "pin set back to default mode" failures were PRE-EXISTING hardware pin-state artifacts (pins 18/24/25 not at pristine power-on defaults), NOT a migration regression — pin 18's full register→OUTPUT→cleanup-restore round-trip through shm metadata proved the backend works. Cleared by resetting pins to defaults (18→alt0 via `gpio -g mode 18 in`; 24/25→alt31 via `pinctrl set N no`) — ✅ 2026-06-06 attempt 1: PASS
- V6: Object/pin registration + cleanup paths (t/100, t/105, t/106, t/150 all green). t/150 initially died at "pin 18 is already in use" (line 36) — root cause was Fix 1 (the `_pin_registration` unregister ownership guard, a branch-only bug NOT from the IPC::Shareable migration). After Fix 1 + the standard pin-reset precondition, 4/4 pass; V5 tests re-verified unbroken — ✅ 2026-06-06 attempt 1: PASS
- V7: Signal-handler + multi-process death/cleanup (cross-process shm sharing). t/153 PASS (3 handlers/uuid). The REAL cross-process tests that drive `t/multi/*.pl` — **t/112 (multi-script meta sharing), t/113 (die), t/114 (sigint)** — ALL PASS, validating masters see slaves' meta writes and cleanup-on-death works through the IPC::Shareable backend. t/154 (4 subtests) + t/155 (self-skip) FAIL/SKIP but are PRE-EXISTING broken tests, **byte-identical on master**, NOT migration regressions: t/154 uses `eval { die }` (caught → no cleanup runs) and asserts pins reset under `fatal_exit => 0`, which its own TODO documents as not cleaning up pins; t/155 self-skips "fatal_exit = 0 TESTS NEED TO BE FIXED". `int_slave.pl`'s "Not a subroutine reference" is also pre-existing (identical on master, no signal lib files differ) and doesn't affect t/114's pass. Broken tests parked as B5 — ✅ 2026-06-06 attempt 1: PASS
- V9: Docs + housekeeping. Updated `Meta.pm` POD: `meta` (notes IPC::Shareable tie-a-scalar / one segment), `meta_key` (CRC32-derived integer, not byte-packed; rpiw→1323166506), `meta_key_check` (string key → CRC32 derivation, existence probe), and corrected the `meta_lock` POD which falsely claimed internal per-transaction locking (fetch/store do NOT lock — callers must wrap). Gate: `perl -c` syntax OK + podchecker pod syntax OK. **Plan-error correction:** `bin/pimeta` + `bin/pimetaerase` DO exist (the plan/B3 wrongly said they didn't) and work unchanged — both ride the `meta_*` shim; verified `bin/pimeta rpit` dumps meta correctly (meta_key=1473559184). FAQ.pod pimeta text is backend-agnostic and accurate → no change needed. build_testing ShareLite scratch files left to B4. **Discovery → V11** (global-destruction warnings when no explicit cleanup) — ✅ 2026-06-06 attempt 1: PASS
- V10: Segment-size headroom + cap behavior (extended the V3 no-XS gate with a `--size` mode, 5 new subtests, total 21). (a) A realistic worst-case blob (40 objects + 40 pins + ~8 user-storage slots) measured **12966 B raw JSON → 14369 B as-stored** (+10.8% double-serialize escaping inflation), only **21.9% of the 65536 B cap**, in exactly **one** segment, round-tripping intact via `is_deeply`. (b) An oversized store (`'x' x (cap+16384)`) **croaks cleanly** with `"Length of shared data exceeds shared segment size"`; the failing store is run in a forked child so its leaked IPC::Shareable lock state dies with it (avoids the lock-deadlock that hung earlier ad-hoc probes), and the parent's independent `meta_fetch` confirms the segment was **left intact** (no truncation/corruption — the croak fires in `_encode_json` before `shmwrite`). **Decision: no `size` bump needed** — real RPi metadata sits ~5× under the cap. Both modes green: plain run = 16/16 (V3 preserved), `--size` = 21/21 — ✅ 2026-06-07 attempt 1: PASS
- V8: Full regression sweep (`make test`, all RPI_* flags). **Migration introduces ZERO regressions** — verified by full failure categorization of 37 files / 120 subtests: t/02-shm_key PASSED in-sweep; every shm/migration test (t/03, t/05, t/110, t/111, t/112, t/113, t/114, t/150, t/153) failed ONLY trailing `rpi_check_pin_status` teardown subtests and passed fully in isolation (V5-V7). Suite not literally green, but every failure is pre-existing & backend-independent: (1) Pi5 pin-status teardown cascade — wiringPi `pinModeAlt` can't set alt 31, so alt-31-default pins can't be restored once touched, cascading across the sweep → **B6**; (2) t/107/108/109 alt-mode/pwm + t/106 pin_map readback, same alt-31 limitation; (3) t/400-407 sysinfo env-format mismatches (df/swap/network/raspi-config on this Pi5/OS) → **B7**; (4) t/154 known-broken → **B5**. The decisive tell: non-shm interrupt tests (t/200-212) fail the SAME 2 trailing pin-status subtests each, proving a universal teardown cascade, not anything in Meta.pm — ✅ 2026-06-06 attempt 1: PASS (migration-clean; suite-green blocked by B5/B6/B7)
- V11: Global-destruction warnings when an object isn't explicitly `cleanup()`d (migration-introduced). **Root cause** (mapped empirically): at global destruction `DESTROY → cleanup → meta_lock → IPC::Shareable::lock → $sem->op` dies because the semaphore is already torn down (`Shareable.pm:524`); that die fires the still-installed `$SIG{__DIE__}` handler, which both echoes the message (`print "$_\n"`) and calls `_cleanup_handler` on a now-undef `$self` (`WiringPi.pm:433`) → two `during global destruction` warnings. Pre-migration ShareLite didn't die at teardown, so implicit cleanup used to work — the migration silently broke it. **Fix:** a weak `%objects` registry (keyed by uuid, populated in `new()` under `_rpi_register`) + an `END` block that runs `cleanup` on any live, not-yet-`{clean}` object **while the segment is still alive** (END phase, before global destruction). This both *restores* working implicit teardown (verified: object_count drops 1→0 at exit with no explicit cleanup) and leaves DESTROY nothing to do. Belt-and-suspenders guards added: `DESTROY` and `_class_signal_handler` both `return if ${^GLOBAL_PHASE} eq 'DESTRUCT'`. Added `use Scalar::Util qw(weaken)`. **Verified:** V11 command emits no warnings; explicit-cleanup and two-object cases clean; regression batch green — t/02/03/05/100/105/110/150/153 (465 tests) + cross-process death t/112-114 + t/153 all PASS (the lone t/110 "got 0 / expected undef" failures were a missing `RPI_PIN_COUNT` env var, **identical on pristine code** via `git stash`, not my change; green once set). `invalid mode 31` / slave-script lines are the known B5/B6 noise — ✅ 2026-06-07 attempt 1: PASS

## Archived Fixes

- Fix 1 (discovered during V6): `_pin_registration` unregister ownership guard was a no-op. `register_pin` stores `users{$uuid}` as a **count** (`...{$requester}++`), but the unregister guard compared that count against the uuid string: `if ($meta->{pins}{$pin_num}{users}{$param{requester}} ne $self->uuid)` → `1 ne $uuid` always true → returned early, never deleting the pin, so a later `register_pin` croaked "already in use". Introduced by branch commit `61eacd7 "fix logic bug"` (3.18 only, NOT on master, predates the migration commits) which inverted an earlier accidentally-working precedence form `(! $count) eq $uuid` (always false → always fell through). Fixed to an existence/ownership check: `if (! exists $meta->{pins}{$pin_num}{users}{$param{requester}})`. `lib/RPi/WiringPi/Core.pm:272`. Resolved 2026-06-06 as part of V6.
