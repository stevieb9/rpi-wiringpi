# Archive: Remove setup_sys() and setup_phys() initialization support

Completed tasks split out of refactor-setup-modes.md. See that file for the live
Validation Table, rules and tracking.

## Archived V Tasks

- V1: Remove the `setup_phys()` dispatch branch (`elsif ($self->_setup =~ /^p/) {...}`) from `RPi::WiringPi::new()` — ✅ 2026-06-04 attempt 1: PASS. Done on branch `3.18` as the coordinated downstream edit for WiringPi::API's V34 (which removed `setup_phys()`). Dropped the `/^p/` branch (`SUPER::setup_phys()` + `pin_scheme(RPI_MODE_PHYS)`); wpi / gpio / else→`RPI_MODE_UNINIT` branches intact. Verified: `grep -n "SUPER::setup_phys\|RPI_MODE_PHYS" lib/RPi/WiringPi.pm` → no output. Full suite run deferred — WiringPi::API 3.1801 not yet installed on this perl (UPGRADE-3.18.md V33 gate).
- V2: Remove the `RPI_MODE_PHYS` branch from `pin_to_gpio()` (Core.pm) and `pin_map()` (Util.pm) — ✅ 2026-06-06 attempt 1: PASS. Dropped both executable `RPI_MODE_PHYS` branches; kept `phys_to_gpio`/`phys_to_wpi` inside the WPI/GPIO branches. The Core.pm POD `RPI_MODE_PHYS` line was also removed under V4, so `grep -n "RPI_MODE_PHYS" lib/RPi/WiringPi/Core.pm lib/RPi/WiringPi/Util.pm` now returns nothing (cleaner than the planned "POD line remains").
- V3: Remove the `export_pin()` and `unexport_pin()` subs from Core.pm — ✅ 2026-06-06 attempt 1: PASS. `grep -n "sub export_pin\|sub unexport_pin" lib/RPi/WiringPi/Core.pm` → no output.
- V4: Remove all setup_sys / setup_phys / SYS / PHYS / export_pin / unexport_pin POD from Core.pm (`pin_scheme`, `pin_to_gpio`, `export_pin`+`unexport_pin` sections) and WiringPi.pm (`export_pin`+`unexport_pin` sections) — ✅ 2026-06-06 attempt 1: PASS. `grep -rn` sweep over lib/ returns nothing; FAQ.pod checked and had no references. podchecker reports "pod syntax OK" for all three modules.
- V5: Update t/106-pin_map.t — removed the `pin_scheme('PHYS_GPIO')` and `pin_scheme('BCM')`/sys assertions; kept default-GPIO, `'GPIO'`, `'WPI'` — ✅ 2026-06-06 attempt 1: PASS. `grep -ni "phys\|sys\|bcm" t/106-pin_map.t` → no output.
- V6: Add a Changes entry noting removal of `setup_phys()`/`setup_sys()` init support, the PHYS and SYS pin schemes, and `export_pin()`/`unexport_pin()` — ✅ 2026-06-06 attempt 1: PASS. Added at the bottom of the open `3.1800 UNREL` section (plan said `2.3634 UNREL`, but the live section is `3.1800 UNREL`); historical entries untouched.
- V7: Final verification sweep across lib/ and the edited test — ✅ 2026-06-06 attempt 1: PASS. `grep -rn "setup_sys\|setup_phys\|RPI_MODE_PHYS\|RPI_MODE_GPIO_SYS\|export_pin\|unexport_pin" lib/ t/106-pin_map.t` prints nothing (exit 1); `podchecker` reports "pod syntax OK" for all three modules (the trailing-whitespace warnings are pre-existing and unrelated to this work).
- V8: Cross-distribution tracking review of `RPi::Const` for `RPI_MODE_PHYS` (3) / `RPI_MODE_GPIO_SYS` (2) — ✅ 2026-06-06 attempt 1: PASS. Reviewed `~/repos/rpi-const` (def `lib/RPi/Const.pm:153-154`, `:mode`/`:all` export, POD `:269-270`, tests `t/27`+`t/30`); this repo clean; no other local consumer. Decision: **KEEP** (published public API + meaningful historical scheme values) — no edits to `RPi::Const`. Full rationale recorded in the plan's Discovery Tracking.
- V9: Cross-distribution tracking review of `RPi::Pin` for any dependency on the removed `setup_phys()`/`setup_sys()` modes — ✅ 2026-06-06 attempt 1: PASS. Reviewed `~/repos/rpi-pin/lib/RPi/Pin.pm`; sweep for the removed symbols → no matches. No `use RPi::Const` in code (POD-only at `:171`), BCM/GPIO numbering only, `mode()` magic ints are pin modes (INPUT/OUTPUT/PWM/CLOCK) not setup schemes, no sys export/unexport. Decision: **nothing to clean up** — no edits to `RPi::Pin`. The magic-int→named-constant modernization stays as backlog B2. Rationale in the plan's Discovery Tracking.

## Archived Backlog

- B2: `RPi::Pin` RPi::Const adoption — ✅ implemented 2026-06-06 in `~/repos/rpi-pin`. `lib/RPi/Pin.pm` now `use RPi::Const qw(:all)`; `mode()` validates against `INPUT`/`OUTPUT`/`PWM_OUT`/`GPIO_CLOCK`, `pwm()` checks `PWM_OUT`, and `pull()` checks `PUD_OFF`/`PUD_DOWN`/`PUD_UP` — all formerly magic integers. Also fixed `pull()`'s inline comment, which had the direction mapping backwards (`0 == down, 1 == up, 2 == off`; correct is OFF 0 / DOWN 1 / UP 2, matching the POD and RPi::Const). Added `RPi::Const => 1.04` to `Makefile.PL` PREREQ_PM (1.05 is UNREL; the constants used all exist in the released 1.04) and a `2.3609 UNREL` Changes entry. Behavior-preserving (constants equal the prior ints); `perl -c` OK; hardware tests SKIP without RPI_SUBMODULE_TESTING in this env. Not promoted to a V# — implemented directly at user request. NOTE: these are edits in the separate `rpi-pin` distribution, committed independently of the rpi-wiringpi V2–V6 work.

## Archived Fixes
