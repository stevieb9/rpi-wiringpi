# Archive: rpi-i2c-fixes plan

Completed work from `plans/rpi-i2c-fixes.md`. Entries are moved here by the
maintenance rules in that plan; never renumber.

## Archived V Tasks

- V1: Sync the stale Mac clone (still 3.1801, missing t/10-validation.t) — ✅ 2026-07-07 attempt 1: PASS (no pull needed — the clone had already been synced since the 2026-07-05 review: after fetch, HEAD = origin/master = `1d2d63f`, 0 ahead/0 behind, status clean, t/10-validation.t present)

- V2: Fix `I2C__readBlockData` XSUB — croak before `sv_setpvn` on `ret == -1` ((STRLEN)-1 over-read/segfault landmine) — ✅ 2026-07-07 attempt 1: PASS (croak mirrors `_readI2CBlockData`'s message but names `_readBlockData()`; rpi2 build warning-free, full suite green (8 tests); croak path exercised live: `_readBlockData(-1, 0, $buf)` dies with the new message, exit 9, no segfault; Changes entry at bottom of UNREL; I2C.xs + Changes scp'd to rpi2 — Mac and rpi2 trees identical; discovery: B4 pin-the-croak-path test)

- V3: `new()` error handling — croak on unopenable device path (was "Can't bless non-reference value"), reword the misleading "not found" ioctl croak, keep `I2C_TESTING`, add HW-free tests — ✅ 2026-07-07 attempt 1: PASS (open failure now croaks "could not open $dev: $!" before the bless and independent of I2C_TESTING; ioctl croak now "ioctl(I2C_SLAVE_FORCE) failed for address 0x%x on $dev: $!" with the escape hatch untouched; 4 new assertions in t/10-validation.t — unopenable-path croak + bless-error gone, and the ioctl croak exercised HW-free via /dev/null (opens fine, ENOTTY on the ioctl) + "not found" wording gone; suite 12 tests green on rpi2; RPi::PWM::PCA9685 verified unaffected — both call sites (new() l.52, reset() l.277) eval-wrap and re-report $@ without parsing it; Changes entry at bottom of UNREL; .pm/.t/Changes scp'd to rpi2 — trees identical)

- V4: POD fixes — `ram=i2c_arm=on` → `dtparam=i2c_arm=on`, "examples direcory" typo, document the 32-byte SMBus block cap on read_block/write_block — ✅ 2026-07-07 attempt 1: PASS (RELEASE_TESTING pod.t green on the Mac after Fix 1; grep confirms every i2c_arm reference is now the dtparam= form and no "direcory" remains; clamp wording verified against the bundled i2c-dev.h — `if (length > 32) length = 32` in both block calls (l.284-285 read, l.303-304 write), so read_block documents the request clamp and write_block the first-32-bytes truncation; rpi2 suite still green (12) after scp of I2C.pm + Changes — trees identical; rpi2's own pod.t skips by design (Test::Pod not installed there; author tests live on the Mac); Changes entry at bottom of UNREL)

- V5: MANIFEST.SKIP skips for build-generated files so post-build RELEASE_TESTING manifest.t stays green — ✅ 2026-07-07 attempt 1: PASS, with corrected scope (the premise was wrong: MANIFEST.SKIP already carried live entries covering every build artifact — `.c$`/`.bs$`/`.o$` — verified two ways: Test::CheckManifest 1.43 source shows MANIFEST.SKIP lines are chomped, compiled as regexes, and matched against $home-relative paths; and the pre-fix run flagged no artifact. The REAL post-build failure was t/10-validation.t missing from MANIFEST (Fix 3) — added in the alpha slot; manifest.t now PASSes post-build on rpi2 via the Fix 2 shim. No MANIFEST.SKIP change was needed or made. Changes entry added; MANIFEST + Changes scp'd — trees identical. Discoveries: V9 — examples/arduino.ino exists and the POD references it, but it was never MANIFEST-listed so tarballs omit it, hidden from manifest.t by the sloppy unanchored `.o$`; B5 — durable Test::CheckManifest install decision)

- V6: Add CI workflow (model: rpi-pwm-pca9685's; ubuntu-only; `make` before `prove -b`) — ✅ 2026-07-07 attempt 1: PASS (.github/workflows/github_ci_default.yml created — adapted for this dist: `master` branches not `main`, explicit "Build the XS dist" step (perl Makefile.PL && make) before `prove -bv t`, the model's RPi::I2C Linux-only comment retained since it literally applies here; YAML parse-validated via ruby/psych; MIN_PERL_VERSION 5.006 confirmed compatible with the 5.10 matrix floor; same 8-perl matrix + 5.32 Coveralls coverage leg as the model; "goes green on push" is only confirmable after the user's next push; workflow + Changes scp'd to rpi2 — trees identical; Changes entry at bottom of UNREL)

- V9: Add examples/arduino.ino to MANIFEST + tighten the loose artifact skip patterns — ✅ 2026-07-07 attempt 1: PASS (arduino.ino MANIFEST-listed in the alpha slot, grep count 1; `.bs$`/`.c$`/`.o$` tightened to their escaped forms and `\.so$` added — the loose `.o$` had been covering .so files only by accident; post-build manifest.t green on rpi2 with I2C.c/I2C.bs/I2C.o present, proving the tightened patterns still exclude the artifacts and arduino.ino's MANIFEST-vs-filesystem state is consistent; the first validation run flagged V6's fresh workflow file → Fix 4 added `^\.github/` per family convention; Changes entry extended; MANIFEST/MANIFEST.SKIP/Changes scp'd — trees identical)

- V7: Unify license metadata per D1 — ✅ 2026-07-07 attempt 1: PASS (D1 decided: artistic_2; Makefile.PL LICENSE `GPL_2` → `artistic_2` (l.18); POD LICENSE AND COPYRIGHT rewritten to Artistic 2.0 with the perlfoundation link, modeled on rpi-pwm-pca9685's section — without copying its "the the" typo — copyright years kept 2017-2026; Mac RELEASE_TESTING pod.t green; grep confirms Makefile.PL and POD agree; Changes entry; synced to rpi2)

- V8: Execute test-coverage-gaps B18 per D2 — coordinated API-contract fix — ✅ 2026-07-07 attempt 1: PASS (read_bytes accumulates into and returns the documented array, ascending from the base register; write_word and process now ($value, [$reg]) with the _set_reg 0x00 default; write_block croaks on >32 bytes — the enforcement half of F9, read_block keeps its documented clamp; POD adjusted (read_bytes per-element -1, write_block croak note); version bumped 3.1802 → 3.1803 per D2 — an initial 3.19 edit was corrected by the user: bumps increment the LAST digit, never the integer after the decimal (rule saved to memory); Changes section renamed 3.1803 UNREL + 4 entries incl. two BREAKING; t/10-validation.t pins flipped + write_word-default/process×2/write_block-cap coverage added; suite 16 tests green on rpi2 after full Makefile regen — XS bootstrap consistent, 00-load diag shows 3.1803; mirror rpi-wiringpi/t/250-i2c_unit.t flipped identically with a requires-≥3.1803 note (runs on the rig Pi after install); cross-plan: test-coverage-gaps F7/F8 marked ✅ RESOLVED (rpi-i2c-fixes V8) in place, B18 retired with pointer; all files synced Mac↔rpi2)

- V10: (was B1) Regenerate the bundled ppport.h with a current Devel::PPPort — ✅ 2026-07-07 attempt 1: PASS (3.20 / perl-5.18.2-era file (6,415 lines) → 3.68 (18,463 lines), generated with the Mac's Devel::PPPort 3.68; self-audit `perl ppport.h I2C.xs` reports "No need to include 'ppport.h'" — I2C.xs uses no API requiring shims; include kept within V10's regenerate-scope, removal logged as B6; clean rebuild on rpi2 against the new header: zero warn/error lines, suite 16 green at 3.1803; Changes entry; ppport.h + Changes scp'd — trees identical)

- V11: (was B2) Address-range validation in new() — ✅ 2026-07-07 attempt 1: PASS (new() now croaks "\$addr 0x%x is out of range - I2C addresses are 7-bit (0x00-0x7F)" for addr > 0x7F, placed after the existing integer check; address 0x00 stays legal by design and is test-pinned: under I2C_TESTING, new(0, '/dev/null') and new(0x7F, '/dev/null') both construct — the RPi::PWM::PCA9685->reset() SWRST general-call dependency; 0x80 and 1024 croak-tested; POD documents the range and the intentional general-call legality; mirror t/250 got the same block with a requires-3.1803 note; Mac pod.t green; rpi2 suite 20 tests green; Changes entry; .pm/.t/Changes scp'd — trees identical)

- V12: (was B3) Confirm RPi::PWM::PCA9685 compatibility with the shipped 3.1803 contract changes; bump its PREREQ_PM floor if needed — ✅ 2026-07-07 attempt 1: PASS (no-op confirmed, exactly as B3 predicted: grep across lib/, t/ and examples/ finds zero call sites of any changed method (read_bytes/write_word/process/read_word); the tightened surfaces stay in-bounds — the only write_block call (PCA9685.pm l.232) sends exactly 4 bytes, well under the new 32-byte croak, and no address can trip V11's range guard: data addrs are self-validated to 0x03-0x77 (l.43) before RPi::I2C is ever called, and reset()'s general-call new(0, ...) (l.277) is the dependency V11 explicitly kept legal; no croak-string coupling — both new() call sites eval-wrap and re-report $@ verbatim, and t/05's regexes match PCA9685's own messages, so V3's reworded croaks are inert; live on rpi2: the stale pre-rename clone ~/repos/rpi-pca9685 fast-forwarded be468c2 → b863cdb (dir name now stale, contents current), full suite green against the rpi-i2c 3.1803 blib — 6 files, 33 tests, perl 5.40.1, loaded $RPi::I2C::VERSION verified 3.1803; caveat: RPi::I2C has never been system-installed on rpi2 (Can't locate in @INC — all prior "suite green on rpi2" runs were blib runs), so no vs-installed baseline was possible; the floor claim rests on the source audit (every method used is contract-identical in 3.1801) plus the dist's Linux CI installing 3.1801 from CPAN; DECISION: PREREQ_PM floor STAYS 'RPi::I2C' => 3.1801 — nothing requires 3.1803, and floor-bumping to an unreleased version would break CPAN installs and the dist's CI for zero gain; zero changes made to rpi-pwm-pca9685)

- V13: (was B4) Pin the V2 `_readBlockData()` croak path in t/10-validation.t — ✅ 2026-07-07 attempt 1: PASS (HW-free regression test appended to t/10-validation.t: `RPi::I2C::_readBlockData(-1, 0, $buf)` — fd -1 forces the underlying ioctl to fail (-1) inside `i2c_smbus_read_block_data`, so the V2 croak guard fires with no bus or device touched; asserts `like $@, qr/_readBlockData() has invalid return/` — pins the pre-V2 (STRLEN)-1 over-read/segfault landmine V2 defused (`_readBlockData` is unused by the .pm but publicly callable, so the guard needs a test). Fresh XS rebuild on rpi2 warning-free (make regenerated I2C.c from I2C.xs); suite green — t/00-load (1) + t/10-validation (20, up from 19; new assertion is test 20) = 21 tests, Result PASS, no segfault. Changes: entry added at the bottom of the 3.1803 UNREL section noting the pinned croak path. t/10-validation.t + Changes scp'd to rpi2 — trees identical. B4 slot retired.)

- V14: (was B5) Decide the release-time RELEASE_TESTING host + durably provision Test::CheckManifest there — ✅ 2026-07-07 attempt 1: PASS (DECISION: **rpi2 under perlbrew 5.42.0** is the RELEASE_TESTING host of record — the only place the XS builds (Makefile.PL is NA on macOS) AND where the full author-test toolchain already lives: Test::CheckManifest 1.43, Test::Pod 1.52, Test::Pod::Coverage 1.10. The B5/Fix-2 premise "Test::CheckManifest absent on rpi2" was imprecise: absent only on the SYSTEM perl /usr/bin/perl 5.40.1 — what non-login `ssh rpi2 cmd` uses, i.e. the V-task validation harness — but present on the perlbrew login-shell perl (Steve's default). So the "durable install" is a NO-OP; nothing to install. Proof: a full `RELEASE_TESTING=1 AUTHOR_TESTING=1 prove -b t` on rpi2 under perlbrew, NO shim, NO PERL5LIB, post-build (perl Makefile.PL && make) — all 5 files RUN (not skip): 00-load/10-validation/manifest.t/pod-coverage.t/pod.t, 24 tests green, Result PASS; `make realclean` after (I2C.c/Makefile are gitignored build artifacts — tree stays clean, git status unchanged). The V5/Fix-2 shim at rpi2:~/tmp-tcm/Test/CheckManifest.pm — staged solely to run manifest.t under the system perl — is now dead: removed (`rm -rf ~/tmp-tcm`, confirmed gone). No repo files changed; decision recorded here + saved to memory. B5 slot retired.)

- V15: (was B6) Remove ppport.h from the dist — ✅ 2026-07-07 attempt 1: PASS (Devel::PPPort self-audit re-run independently: `perl ppport.h I2C.xs` → "No need to include 'ppport.h'" (and it emits the exact one-line patch dropping the include), confirming I2C.xs uses no shimmed API. Removed `#include "ppport.h"` from I2C.xs — only the perl core headers (EXTERN/perl/XSUB) + i2c-dev.h remain; dropped the `ppport.h` line from MANIFEST; deleted the file from both trees (`rm` → unstaged `D`; it was tracked, committed at 3.68 in 01a7f20). Only 3 ppport references existed dist-wide (I2C.xs:5, MANIFEST:18, and the historical V10 Changes note) — none in MANIFEST.SKIP, no typemap. Validated on rpi2 under perlbrew 5.42.0: clean rebuild WITHOUT ppport.h — zero warn/error, fresh I2C.c carries 0 ppport references (xsubpp no longer pulls it in); full RELEASE_TESTING green — 00-load/10-validation/manifest.t/pod-coverage.t/pod.t, 24 tests, Result PASS (manifest.t confirms MANIFEST↔tree stay consistent after the dual removal); `make realclean` after. Changes: bottom-of-UNREL entry noting the removal supersedes the V10 3.68 regeneration (both within the unreleased 3.1803). I2C.xs/MANIFEST/Changes scp'd + ppport.h rm'd on rpi2 — contents synced. DISCOVERY (commit state): Mac HEAD 01a7f20 "Major cleanup/fixes" = the V2-V12 bundle, and `git ls-remote origin master` shows it is ALREADY pushed → the V6 CI is already live/has run; only V13 (B4) + V15 (B6) remain uncommitted on the Mac. rpi2 stays a content-synced test rig at 1d2d63f. B6 slot retired — backlog now empty.)

## Archived Fixes

- Fix 1: problem discovered during V4 — RELEASE_TESTING pod.t on the Mac returned NOTESTS ("No files found in (blib)"): a stale June-12 `blib/` skeleton from an NA'd build attempt sat in the Mac clone, and Test::Pod's `all_pod_files_ok()` prefers blib over lib whenever blib exists. Removed the artifact (git-ignored, contained no .pm files, cannot regenerate on macOS since Makefile.PL exits NA on non-Linux); pod.t then scanned lib/ and passed. — ✅ 2026-07-07 resolved in-task

- Fix 2: problem discovered during V5 — Test::CheckManifest not installed on rpi2 (t/manifest.t skips NOTESTS), so V5's validation command couldn't run. Resolved for validation by staging the Mac's Test::CheckManifest 1.43 (single pure-Perl file, core-only deps) at rpi2:~/tmp-tcm/Test/CheckManifest.pm and running with `PERL5LIB=~/tmp-tcm`; the durable install decision is B5. — ✅ 2026-07-07 resolved in-task

- Fix 3: problem discovered during V5 — the actual post-build manifest.t failure: t/10-validation.t was never added to MANIFEST when the test file landed (`1d2d63f`), so a 3.1802 tarball would have shipped without it. Added it (alpha slot after t/00-load.t); manifest.t green. — ✅ 2026-07-07 resolved in-task

- Fix 4: problem discovered during V9 — the validation run flagged .github/workflows/github_ci_default.yml (just added by V6): the new dotdir was neither MANIFEST-listed nor skip-listed. Added `^\.github/` to MANIFEST.SKIP per the family convention (rpi-pwm-pca9685 carries the same entry — duplicated there at its lines 37-38, FYI); CI config stays out of the tarball. — ✅ 2026-07-07 resolved in-task
