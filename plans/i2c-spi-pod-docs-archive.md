# Archive: i2c-spi-pod-docs plan

Completed work from `plans/i2c-spi-pod-docs.md`. Entries are moved here by the
maintenance rules in that plan; never renumber.

## Archived V Tasks

- V1: rpi-adc-ads: add ON THE WIRE + DEVICE SPECIFICS (I2C, ADS1x15) per checklist — ✅ 2026-07-06 attempt 1: PASS (podchecker clean — including fixing a pre-existing whitespace-only-line warning under `register`'s Parameters; section grep count 2; all facts verified against SBAS473C/SBAS444B high-res renders; Changes entries added)

- V2: rpi-adc-mcp3008: add ON THE WIRE + DEVICE SPECIFICS (SPI, MCP3008) per checklist — ✅ 2026-07-06 attempt 1: PASS (pod OK; section grep count 2; full-duplex 3-byte frame, control table, timing and electrical facts verified against DS21295D pp.1/4/19/21 high-res renders; existing CHANNEL SELECT table confirmed correct incl. IN+/IN- pairings; Changes entry added)

- V3: rpi-bmp180: add DEVICE SPECIFICS + REGISTER MAP + ON THE WIRE + DATASHEET (I2C via WiringPi::API devLib) — ✅ 2026-07-06 attempt 1: PASS (pod OK; section grep count 4; transactions taken from ~/repos/WiringPi/wiringPi/bmp180.c and verified against BST-BMP180-DS000-09 pp.7/13/18/20/21 renders + operating-conditions text extract; datasheet URL curl-verified 200/PDF; documented that every temp()/pressure() call runs the full ~10ms temp+pressure cycle; Changes entry added)

- V4: rpi-dac-mcp4922: add ON THE WIRE + DATASHEET (SPI; register docs + DEVICE SPECIFICS exist) — ✅ 2026-07-06 attempt 1: PASS (pod OK; section grep count 2; 16-bit frame verified against DS22250A pp.24-25 renders (Registers 5-1/5-2/5-3, Figures 5-1/5-2/5-3), pin table p.17 confirms no SDO (one-way bus), 1MHz clock confirmed in WiringPi::API spi_setup default; datasheet URL curl-verified 200/PDF; Changes entries added)

- V5: rpi-digipot-mcp4xxxx: add ON THE WIRE + DEVICE SPECIFICS (SPI; register docs + datasheet exist) — ✅ 2026-07-06 attempt 1: PASS (pod OK; section grep count 2; command byte, pot-select codes, multiple-of-16-clocks abort rule, CS-rise execution, mid-scale power-up, shutdown semantics and pinouts verified against DS11195C pp.1/17/18 renders + AC-spec text (fSCK 10MHz); worked set(127)/shutdown(2) byte examples match the module's t/set_shutdown.t expectations; Changes entry added)

- V6: rpi-eeprom-at24c32: add DEVICE SPECIFICS + MEMORY MAP + ON THE WIRE + DATASHEET (I2C) — ✅ 2026-07-06 attempt 1: PASS (pod OK after fixing my own signature-heading links (read($addr) etc. need the L<text|/"section"> form); section grep count 5 ≥ 4; frames verified against doc0336 pp.5/8/9/10 renders — byte-write, address-load + current-address read (STOP-divided variant of the datasheet's random read, valid because the address counter persists), t_WR 10ms max vs the module's 1ms default delay documented; datasheet URL curl-verified 200/PDF; Changes entry added)

- V7: rpi-gpioexpander-mcp23017: add DEVICE SPECIFICS + REGISTER MAP + ON THE WIRE + DATASHEET (I2C; cross-ref RPi::Const :mcp23017_registers) — ✅ 2026-07-06 attempt 1: PASS (pod OK — again needed L<text|/"section"> links for signature headings; section grep count 4; register table verified against DS20001952C Table 3-5 p.17-18 render (matches RPi::Const values and cleanup()'s POR restore), features/pinout p.1, 25mA/125mA limits and IPU 40-115uA pull-up spec from text (correctly avoided the folk "100k" figure — this datasheet specs a current); documented the XS's IOCON write-lock (hardware R/W, module locks it to protect the BANK=0 map); datasheet URL curl-verified 200/PDF; Changes entry added)

- V9: rpi-oled-ssd1306: add DEVICE SPECIFICS + COMMAND SET + ON THE WIRE + DATASHEET (I2C control/data framing) — ✅ 2026-07-06 attempt 1: PASS (executed before V8, which is deferred on rpi-i2c-fixes; pod OK; section grep count 4; control byte semantics/slave address/GDDRAM verified against SSD1306 rev 1.1 pp.19-20/25 text, command opcodes cross-checked header-vs-Table 9-1, charge pump 8Dh/14h from the app-note section, t_cycle 2.5us (400kHz) from Table 13-6; dist had unrelated uncommitted work (wiringPi min-version) — left untouched, Changes entry appended after it; documented the ~0.3s full-refresh cost of byte-per-frame pushes)

- V10: rpi-rtc-ds3231: add DEVICE SPECIFICS + REGISTER MAP (0x00-0x12) + ON THE WIRE + DATASHEET (I2C) — ✅ 2026-07-06 attempt 1: PASS (pod OK; section grep count 4; every fact verified against DS3231 datasheet 19-5170 Rev 10 renders pp.1/11/12/15/16/17 — Figure 1 register map matches the XS masks exactly, hour-register bit 6 = 12/24 (high = 12-hr) and bit 5 = AM/PM (high = PM)/20-hour, century bit 7 of 0x05, 10-bit 0.25C temp encoding, snapshot buffers re-latch on any START, pointer persists across STOP (validates the module's STOP-divided pointer-write + read); analog.com blocks curl at the TLS layer so the POD links the Adafruit-hosted copy (product-files/3013, curl-verified 200/PDF); Fix 3 resolved in-task; working tree was clean — the plan's uncommitted-DS3231.xs warning was stale; Changes entries added; discoveries: V15 (POD typos), B13 (non-atomic composed ops), B14 (verify DS1307 claim))

- V13: rpi-adc-ads: DATA RATE head3 documented only ADS101x rates; add per-family rate table — ✅ 2026-07-06 attempt 1: PASS (pod OK; 860SPS grep count 3 ≥ 2; both tables verified against renders — SBAS473C p.16 (ADS101x 128SPS-3300SPS, chip POR default 100=1600SPS) and SBAS444B p.19 (ADS111x 8SPS-860SPS, chip POR default 100=128SPS); the "(default)" marker in the POD tables means the module's DR=000 write, consistent with the sibling GAIN table's convention; added the 8SPS/~125ms slow-read warning for 16-bit parts; tree was clean (V1 edits committed); Changes entry added; discovery: B15 (bits 4/2 undocumented, per-model field applicability))

- V14: rpi-bmp180: POD fixes — SYNOPSIS constructor missing ->new, temp() misspelled Fahrenheit/Celsius — ✅ 2026-07-06 attempt 1: PASS (pod OK; all three target strings present exactly once — RPi::BMP180->new, Fahrenheit, Celsius — and no farenheit/celcius remain; the Expected "count ≥ 3" was mis-specified: `grep -c` counts matching LINES and pod2text wraps "Fahrenheit ... Celsius" onto one line, so it returns 2 — `grep -oE | wc -l` gives the intended 3; SYNOPSIS now `RPi::BMP180->new($base)` matching the new($pin_base) method and the temp() default (Fahrenheit) is consistent with the SYNOPSIS `$f = $bmp->temp`; edited alongside the still-uncommitted V3 sections in the same file — my own prior work, so continued rather than left untouched; Changes entry appended after V3's)

- V15: rpi-rtc-ds3231: POD typo fixes — SYNOPSIS `$rt->sec`, temp() celcius/farenheit, "Auxillary" head1 + matching code comment — ✅ 2026-07-06 attempt 1: PASS (pod OK; `grep -c` count 6 ≥ 4, no line-collapse this time; all four fixed and verified — `$rtc->sec`, Celsius, Fahrenheit, Auxiliary — with no auxillary/farenheit/celcius-in-POD or `$rt->` remaining; edited alongside my own uncommitted V10 sections in the same file; Changes entry appended after V10's; discovery B16 — the local variable `$celcius` in temp() (lines 29-30) is still misspelled but is a code identifier outside this POD-typo task's scope, logged not fixed)

- V12: Cross-repo sweep (run last) — in-scope dists podchecker-clean, every required section present, Changes entry in each — ✅ 2026-07-07 attempt 1: PASS (9-dist form: rpi-i2c dropped (V8 still deferred on rpi-i2c-fixes), rpi-spi dropped (user skip); all 9 .pm files report "pod syntax OK" and all 9 have a Changes 'wire' entry; DEVICE SPECIFICS + ON THE WIRE present in all 9, a register-equivalent present in all 9 (REGISTERS / CHANNEL SELECT / REGISTER MAP / DEVICE REGISTER / CONTROL BYTE / MEMORY MAP / COMMAND SET); `=head2 DATASHEET` in the 6 that needed one, and the inline datasheet link confirmed in the 3 pre-existing — rpi-adc-ads (adafruit ads1015.pdf), rpi-adc-mcp3008 + rpi-digipot-mcp4xxxx (self-hosted github.io PDFs) — whose migration to a DATASHEET head2 stays parked as B3; no code edits, no commit)

- V16 (promoted from B3): Localize every datasheet reference — bundle each dist's datasheet PDF under its own `docs/` and repoint POD at the local file via `F<...>` instead of a web URL, across all 10 in-scope dists (a superset of B3's original 4) — ✅ 2026-07-07 attempt 1: PASS (user expanded B3 from the 4 pre-existing-link dists to all 10. 6 PDFs fetched fresh, each HEAD-verified 200/PDF and identity-checked against page 1 before citing: rpi-adc-ads ads1015.pdf (SBAS473C), rpi-pwm-pca9685 PCA9685.pdf (NXP Rev. 4), rpi-bmp180 BMP180.pdf (BST-BMP180-DS000-09), rpi-eeprom-at24c32 AT24C32.pdf (doc0336), rpi-oled-ssd1306 SSD1306.pdf (rev 1.1), rpi-rtc-ds3231 DS3231.pdf (19-5170 Rev 10). 4 reused already-bundled copies: rpi-adc-mcp3008 MCP3008.pdf (DS21295D), rpi-digipot-mcp4xxxx mcp4xxxx.pdf, rpi-dac-mcp4922 mcp49xx.pdf (DS22250A), rpi-gpioexpander-mcp23017 docs/MCP23017-Data-Sheet-DS20001952.pdf. POD now uses `F<docs/...>` throughout (rpi-pwm-pca9685 also lost its SEE ALSO URL; rpi-digipot intro became an `L</DATASHEET>` cross-ref). All 10 podchecker-clean with zero datasheet web-URLs remaining; each PDF added to / confirmed in MANIFEST; Changes entry in each. Discoveries resolved in-task: rpi-gpioexpander shipped rev D but POD cited rev C (corrected to the shipped DS20001952D; register map unchanged C→D so V7's facts hold) and its bundled PDF was missing from MANIFEST (added), plus a 'datasheed' Changes typo fixed. The model dist rpi-pca9685 was renamed to rpi-pwm-pca9685 / RPi::PWM::PCA9685 by a concurrent session mid-task — the B3/V16 edits survived the move (verified in the new lib/RPi/PWM/PCA9685.pm). Spun off B17 (rpi-dac-mcp4922 ships an unreferenced mcp48xx.pdf); B4 superseded by this in-repo approach. No commits.)

- V17 (promoted from B5): rpi-adc-ads — tighten model() validation so it stops admitting the SPI-only ADS1018/ADS1118 (an I2C driver can't drive them) — ✅ 2026-07-07 attempt 1: PASS (the regex `/^ADS1[01]1[3458]/` admitted eight parts; the family's I2C members are only the six ADS1013/14/15 (12-bit) + ADS1113/14/15 (16-bit), the `8` variants being the SPI counterparts. Re-verified against the bundled SBAS473C (ads1015.pdf): it describes 1013/14/15 over an "I2C-compatible serial interface" and never mentions a 1018, and the dist's own DEVICE SPECIFICS POD already says "six-part family". Fixed three spots — the regex ([3458]→[345]), the die message ("3, 4, 5 or 8"→"3, 4 or 5"), and the POD valid-values line (was the bare regex; now enumerates the six parts and notes 1018/1118 are rejected) — plus t/25-model.t (moved 18 out of the legit loop and added ADS1018/ADS1118 to the faulty set, so it now asserts they die). Verified the regex logic standalone — 6/6 accept, 9/9 reject — because the XS-backed test can't run on the Mac; podchecker clean. Edited atop the dist's uncommitted 1.04 work; Changes entry added; no commit.)

- V18 (promoted from B9): rpi-dac-mcp4922 — fix three latent XS/API quirks — ✅ 2026-07-07 attempt 1: PASS (three fixes: (1) _set()'s clear-mask cleared `0xFFF >> lsb` (the low bits) instead of the full 12-bit data field, so on the 8/10-bit parts (lsb 4/2) — where the data window `data << lsb` sits at the top of the field — stale top-of-field bits in an incoming cached register would survive the OR; now clears bits 11-0 (`(int)pow(MULT,12)-1`). Verified by a standalone calc: worst-case 8-bit stale field gives 0x7F50 (old, wrong) vs 0x7A50 (new, correct). (2) Dropped set()'s dead `my $buf =` capture of the void _set(). (3) register()'s POD claimed a live view of the register "at various stages", but the cache is written once in new() and never updated by set(); reworded to say it returns the construction-time BUF/GAIN/SHDN base, not set() writes (chose the POD-reword option over updating the cache, which would need _set changed from void to return its word). The bug was latent because register() never feeds a stale cache back into _set. XS can't be compiled/tested on the Mac (Pi build pending); pod OK; edited atop the dist's uncommitted work incl. this session's datasheet localization; Changes entries added; spun off B19 (expose _set's word-building for HW-free testing). No commit.)

- V19 (promoted from B10): rpi-eeprom-at24c32 — replace the fixed post-write sleep with datasheet acknowledge polling — ✅ 2026-07-07 attempt 1: PASS (the XS slept a fixed `delay` ms (default 1) after each byte write, but t_WR is 10ms max at Pi voltages (doc0336 "Self-Timed Write Cycle (10 ms max)"), so the 1ms default under-waited and a following read/write could land in a still-running cycle and fail. Verified the ACK-poll procedure on doc0336 p.9 (bundled AT24C32.pdf): the chip NACKs its address during the internal write and ACKs once done. eeprom_write() now re-addresses the chip with `i2c_smbus_write_quick(fd, I2C_SMBUS_WRITE)` (confirmed declared in the bundled i2c-dev.h; reuses the I2C_SLAVE-set address on fd, no data, no side effects) until it ACKs — waiting the real t_WR — capped at `write_cycle_time` ms (the `delay` param) but floored at 15ms (>t_WR) so a too-small delay never under-waits and a wedged/absent chip bails instead of spinning. `delay` kept in the API (backward-compatible) as a rarely-needed poll ceiling; POD (param doc + ON THE WIRE passage) reworded. XS can't build on the Mac; verified what's checkable here: clang -Wall syntax-check of a faithful mirror of the new eeprom_write tail (clean), a poll-logic simulation (healthy 8ms→8ms, worst 10ms→10ms, wedged→15ms floored cap, delay=50→50ms honored), podchecker clean. Pi build + real-chip validation pending. Changes entry added; edited atop this session's localization work; no commit.)

- V20 (promoted from B11): rpi-gpioexpander-mcp23017 — stop locking IOCON as read-only; allow its functional bits, croak only on the BANK bit — ✅ 2026-07-07 attempt 1: PASS (the XS write-guard listed IOCON (0x0A/0x0B) in readOnlyRegisters alongside the genuinely read-only INTF (0x0E/0x0F) and INTCAP (0x10/0x11); but IOCON is R/W (DS20001952D) and its functional bits (MIRROR/SEQOP/DISSLW/HAEN/ODR/INTPOL) are wanted, esp. for interrupts. Chose B11's option 2 — unlock + BANK-bit croak — over silent masking, so an unsupported BANK=1 fails loud. Datasheet-confirmed BANK = bit 7 (0x80). Removed 0x0A/0x0B from both readOnlyRegisters arrays (made them []-sized so the sizeof loop stays correct); added _checkIoconBank(reg, value), called from GPIO_setRegister (the sole write choke point — register() is the only IOCON writer, register_bit() is a getter), which croaks if reg is 0x0A/0x0B and value has bit 7 set; reworded the REGISTER MAP POD note; updated t/05-registers.t (IOCON moved out of the read-only loop; added coverage that a non-BANK IOCON write succeeds and a BANK write croaks on both mirror addresses). Verified: a clang truth-table mirror of the new guard (13/13 cases correct — IOCON functional bits allowed, BANK + INTF/INTCAP blocked); the XS compiles clean on rpi2 (aarch64) into MCP23017.so; the updated test syntax-checks against the built module. Runtime IOCON behaviour still needs a real chip (t/05 is skip_all without RPI_MCP23017 + a chip). Changes entry added; no commit.)

- V8: rpi-i2c — add DEVICE SPECIFICS (bus-level) + ON THE WIRE (generic, per-method) + DATASHEET (UM10204) — ✅ 2026-07-08 attempt 1: PASS (unblocked once plans/rpi-i2c-fixes.md's own V8 landed: rpi-i2c is now 3.1803 with the method contracts settled — read_bytes() returns the documented array, write_word()/process() are ($value,[$reg]). Added a TECHNICAL INFORMATION head1 to lib/RPi/I2C.pm: DEVICE SPECIFICS as bus-level facts — SDA=GPIO2/SCL=GPIO3 on /dev/i2c-1, 1.8k pull-ups to 3.3V, 3.3V logic, 7-bit addressing (0x00 general-call legal), 100kHz default / 400kHz via dtparam=i2c_arm_baudrate, 32-byte SMBus block cap; ON THE WIRE with per-method SMBus/I2C transaction diagrams (read/read_byte/read_bytes/read_word/read_block/write/write_byte/write_word/write_block/process) drawn against the settled 3.1803 contracts, S/Sr/P/A/N legend, 7-bit-addr-shifted example (0x04→0x08 write/0x09 read), words low-byte-first; DATASHEET citing NXP UM10204 Rev. 7.0 (1 October 2021). No register section — bus transport has no chip registers. Verified: podchecker clean, `pod2text | grep -cE 'ON THE WIRE|DATASHEET|DEVICE SPECIFICS'` = 3, make test PASS. Fresh 11-dist sweep confirmed all in-scope dists carry DEVICE SPECIFICS + ON THE WIRE + register-equivalent + DATASHEET (rpi-i2c REG n/a as a transport). The UM10204 PDF could not be bundled locally — the canonical NXP URL 404s (verified via curl: HTTP/2 404) and no clean verified mirror was found, so the DATASHEET cites the document by identity rather than F<...>; local-bundle spun off as B20. Changes entry added; no commit.)

## Archived Fixes

- Fix 1: discovered during V1 — the `CONVERSATION BIT` head3 documented the OS bit's read semantics inverted vs the datasheet (SBAS473C p.15: reads 0 while converting, 1 once idle; the POD claimed the opposite), and the ADS.xs fetch() comment repeated the same error ("wait for bit 15 ... to go false" — the code correctly waits for it to read 1). Blocked V1 because the new ON THE WIRE poll description would contradict adjacent text. Fixed: head3 renamed to `CONVERSION BIT`, prose corrected ("conversation" → "conversion", read semantics per datasheet, "occuring" typo), XS comment rewritten. — ✅ 2026-07-06 (during V1)

- Fix 2: discovered during V4 — rpi-dac-mcp4922's SHUTDOWN BITS prose claimed the register SHDN bit shuts down "both DACs on the chip"; DS22250A Register 5-1 says it shuts down only the selected channel (which also matches the module's own per-DAC disable_sw()). The earlier datasheet-audit had fixed the table's polarity but not the prose. Blocked V4 (the new ON THE WIRE disable_sw frame documents per-channel shutdown). Fixed: prose rewritten (selected-DAC wording, 500k typical Vout impedance, pin-vs-bit distinction), table Result column reworded. — ✅ 2026-07-06 (during V4)

- Fix 3: discovered during V10 — rpi-rtc-ds3231's POD was never podchecker-clean: hour()'s L</clock_hours> link can't resolve (the heading is the signature form clock_hours([$format])), and an empty =head1 METHODS sat directly above =head1 Operational Methods, drawing an empty-section warning. Both blocked V10's pod-OK gate. Fixed: link rewritten to the L<text|/"section"> form, the empty METHODS head1 removed (nothing linked to it), Changes entry added. — ✅ 2026-07-06 (during V10)
