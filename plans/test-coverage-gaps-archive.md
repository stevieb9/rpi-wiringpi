# Archive — completed V tasks and resolved fixes

## Archived V Tasks

- V1: RPi::Const — self-policing coverage guard for all export tags/constants (closes the untested `:altmode` + `:mcp23017_pins`) — ✅ 2026-06-23 attempt 1: PASS. Added `~/repos/rpi-const/t/50-const_coverage.t`: a `%expected` manifest (12 tags, 80 constants, names+values) checked against the live `%RPi::Const::EXPORT_TAGS`/`@EXPORT_OK` via `is_deeply` — so a new/removed tag (check 1), a new/removed constant in any group (check 2), a wrong value (check 3), or an orphan/dup in `:all` (check 4) all fail; plus the `:edge`≡`:int_edge` invariant (check 5). 178 subtests pass; full dist suite 268 pass. **Falsified**: removing `ALT5` from the manifest (simulating a new untested constant) goes RED on the altmode list check. Also added dedicated per-tag files `t/28-const_altmode.t` and `t/48-const_mcp23017_pins.t` to match the existing per-tag convention (every other tag has its own file). Full dist suite 18 files / 290 tests PASS. Lives in rpi-const only (not mirrored into rpi-wiringpi, per user). Changes 1.07 UNREL noted; uncommitted (user commits).

- V2: RPi::Pin — ungated HW-free validation coverage, mirrored into rpi-wiringpi — ✅ 2026-06-23 attempt 1: PASS. Added `rpi-pin/t/10-validation.t` (23 tests, `NO_BOARD=1` + eval/like, no Test::Fatal prereq): `new` integer-validation dies (undef/'x'/-1/'1.5'/''), valid `new` isa + `num`/`comment` accessors, `mode`/`write`/`pull` bad-arg dies, `pwm` non-root die (SKIP under root), the 3 `set_interrupt` + 3 `background_interrupt` croaks (edge/callback/debounce — validated before any fork), and `interrupt_set` delegation. Mirrored verbatim to `rpi-wiringpi/t/116-pin_validation.t` (uses installed RPi::Pin, no RPiTest, no shm → independent of the serial suite + t/110-114 counts); both pass ungated with no Pi. Surfaced F18 (`mode_alt` has no input validation → B13). rpi-pin Changes 3.1802 UNREL noted; uncommitted (user commits).

- V3: RPi::DigiPot::MCP4XXXX — `set()`/`shutdown()` unit coverage + F4 fix, mirrored into rpi-wiringpi — ✅ 2026-06-23 attempt 1: PASS. Added `rpi-digipot-mcp4xxxx/t/set_shutdown.t` (13 tests): mocks all five imported wiringPi calls (so it runs ungated, off-board) and uses a shared `@events` log via Mock::Sub `side_effect` to assert the exact SPI control/data bytes (set → `[0x11,127]`/`[0x13,255]`; shutdown → `[0x21,0]`/`[0x22,0]`), the CS-LOW→spiDataRW→CS-HIGH order, and the data(0-255)/pot(1-3) croaks (no SPI write on a bad data arg). **F4 fixed**: `shutdown()`'s bad-pot die said `"set()"` (copy-paste) → now `"shutdown()"`, asserted in the dist test. Mirrored to `rpi-wiringpi/t/346-dpot_unit.t` (12 tests; tests the installed module, ungated, version-agnostic croak regex; t/345 still owns the hardware sweep). Both pass with no Pi; full dist suite 25 PASS. dist Changes 3.1802 UNREL noted; uncommitted (user commits).

- V4: RPi::DAC::MCP4922 — first functional tests (pure word-builders + validation), mirrored into rpi-wiringpi — ✅ 2026-06-23 attempt 1: PASS. Built the XS dist, then added `rpi-dac-mcp4922/t/register.t` (42 tests, HW-free): the pure Perl-callable XS builders `_reg_init` (0x1000/0x3000/0x5000/0x7000 for buf/gain combos, SHDN always on) and `__set_dac` (bit 15 set/clear, preserves control bits); the model→bits→lsb chain (`_model` MCP4922/4912/4902→12/10/8, last-4-digit regex, invalid/missing-model dies; `_lsb`/`_data_lsb`=12−bits); accessor validation (`_buf`/`_channel`/`_gain`/`_cs`/`_shdn_pin` out-of-range dies + buf/gain defaults — all fire before any wiringPi call); `register()` set/get/default; enable/disable no-DAC + no-SHDN guards; and `new()` arg-validation dying before hardware. Mirrored to `rpi-wiringpi/t/311-dac_unit.t` (42 tests; installed module, ungated, no build/no Mock::Sub; t/310 keeps the hardware sweep). Both pass with no Pi. Core `_set` math left for B1 (writes-and-returns-void). No bug found. dist Changes 3.1802 UNREL noted; uncommitted (user commits).

- V5: RPi::ADC::ADS — register/validation unit coverage + F1/F19 fixes, mirrored into rpi-wiringpi — ✅ 2026-06-23 attempt 1: PASS. Built the XS dist. **F1 fixed**: `t/925-bitwise_gain.t` had a stray mid-file `done_testing(); exit;` dead-coding the `gain(8)` bad-param block — removed, so the croak now runs. **F19 fixed**: `register()`'s MSB out-of-range message said `"msg"` (typo) → `"msb"`. Added `t/26-register.t` (register() validation: missing-lsb, msb/lsb 0..255 bounds incl. negatives; set→`bits` round-trip; `_bit_set` field clear/set/preserve → 0xF3FF/0xF1FF) and `t/56-samples_validation.t` (`_samples()` rejects 0/negative/non-int, returns the value, falls back to default 1 on undef). Mirrored the register/bits/_bit_set/gain/_samples checks to `rpi-wiringpi/t/143-adc_unit.t` (13 tests; installed ADS 1.03, ungated, version-agnostic regex for the F19 croak; t/140-142 keep the hardware reads). All HW-free, no Mock::Sub; full ADS dist suite 274 PASS. FSR/scaling math left for B2. dist Changes 1.04 UNREL noted; uncommitted (user commits).

- V6: RPi::GPIOExpander::MCP23017 — pinBit/validator unit coverage + F2/F3 fixes, mirrored into rpi-wiringpi — ✅ 2026-06-23 attempt 1: PASS. **F2 fixed**: `GPIO__pinBit`'s OOB croak (`MCP23017.xs:134`) had a `%d` with no argument (printed garbage) → now passes `pin`. **F3 fixed**: `t/35-pullup.t` + `t/40-pullup_bank.t` tested `mode_bank` in their bad-param/no-state blocks → corrected to `pullup`/`pullup_bank` (HW-gated, fixed by inspection). Built the XS dist; added `t/02-validation.t` (26 tests, ungated): `_pinBit` map (0/7/8/15 → 0/7/0/7) + OOB croak naming the pin (proves F2); `_check_bank`/`_check_mode`/`_check_pullup` accept 0/1 & reject numeric out-of-range; `_check_write` undef/2 croaks. Noted: the numeric `!=` checks intentionally let a non-numeric string numify to 0 (not a V6 target). Mirrored to `rpi-wiringpi/t/331-mcp23017_unit.t` (25 tests; installed module, ungated, version-agnostic pinBit croak). Installed the rebuilt dist so the rig carries F2. bit.c direct math deferred to B14 (not Perl-callable). dist Changes 1.04 UNREL noted; uncommitted (user commits).

- V7: RPi::EEPROM::AT24C32 — un-gate the validator croaks + surface F6 — ✅ 2026-06-23 attempt 1: PASS. The validator croaks were only reachable via the RPI_EEPROM-gated integration tests (t/420-422, which build a hardware-backed `$pi->eeprom` object). Since `_check_addr`/`_check_byte` are plain package subs that croak before any I2C, added `rpi-wiringpi/t/423-eeprom_validation.t` (14 tests, ungated, no RPiTest/shm): `_check_addr` undef/-1/4096 croaks + 0/4095 accepted + missing-caller croak; same shape for `_check_byte` (0..255); and an **F6** probe — `new(device => '/nonexistent')` returns an object with `fd == -1` and does not croak (eeprom_init's `-1` is swallowed after a `fprintf(stderr)`; the stderr is redirected to /dev/null via fd-2 save/restore so the test stays quiet). t/421 remains as the gated hardware integration version. **No dist code change** (F6's fix is a `new()` contract change → deferred to B15). Verified HW-free. No Changes entry (test-only, canonical-suite addition).

## Archived Fixes

_None yet._
