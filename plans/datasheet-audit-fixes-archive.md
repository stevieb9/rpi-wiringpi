# Archive — completed V tasks and resolved fixes

## Archived V Tasks

- V1: DS3231 `setMonth()` raw-binary→BCD (`~/repos/rpi-rtc-ds3231` `DS3231.xs`) — ✅ 2026-06-22 attempt 1: PASS. Fixed via a new datasheet-grounded `setBcdField(fd,reg,value,keep,name)` helper (keep `0x80` = Century); months 10/11/12 now store `0x10/0x11/0x12` not `0x0A/0x0B/0x0C`. Compile-verified (`perl Makefile.PL && make`, no warnings) + standalone logic test (months 1-12 valid BCD, round-trip, century preserved). Hardware round-trip + commit/release pending (rig dark; user commits).
- V2: DS3231 `setHour()` 12-hour raw→BCD (`DS3231.xs`) — ✅ 2026-06-22 attempt 1: PASS. Same `setBcdField` path (keep `0x60` = 12/24 select + AM/PM); 24-h path keep `0x00` (bit6=0). Logic-verified (12-h hours 1-12 round-trip with flags preserved; 24-h 0-23 with bit6=0).
- V3: DS3231 negative-temperature sign (`DS3231.xs` `getTemp`) — ✅ 2026-06-22 attempt 1: PASS. MSB now read as `(int8_t)` and decoded `(int8_t)msb + (lsb>>6)*0.25` per DS3231 Rev 2 temp registers; sub-zero decodes correctly (−24.75 verified) while positive unchanged (25.25).

- V4: AT24C32 `eeprom_write_block` broken & exported (`~/repos/rpi-eeprom-at24c32` `AT24C32.xs`) — ✅ 2026-06-22 attempt 1: PASS. Removed the function and its XS export rather than "fix" it: signature was single-byte `(fd,mem_addr,data)` (a byte-for-byte duplicate of `eeprom_write`, so "block" was a misnomer), it never transmitted `addr_msb`, and it handed a 2-byte `buf` to `_writeByte` which reads `buf[2]` OOB. Off the OO path — the module's `write()` uses `eeprom_write`. Rebuilt clean (no warnings); `->can('eeprom_write_block')` now false. Hardware round-trip on the unchanged `write`/`read` path PASS at `delay => 10` (slots 200-205; `delay => 1` hit a bus-timing `Remote I/O error`, unrelated to the change). Changes 1.00 UNREL noted; uncommitted (user commits/releases).
- V5: ADS1115 constructor drops the `gain` arg (`~/repos/rpi-adc-ads` `ADS.pm:172`) — ✅ 2026-06-22 attempt 1: PASS. `$self->gain($args{mode})` → `$self->gain($args{gain})`: the old code ignored a `gain =>` arg (always left DEFAULT_GAIN) and a `mode =>` arg corrupted the gain. Falsified per the step-2 protocol (reverted → RED on every non-default gain, fix → GREEN). Tests added: standalone `t/02-new_gain.t` (11, hardware-free — `new(gain=>0..7)` register bits + the mode-leak guard) and rpi-wiringpi `t/142-adc_gain.t` (50 — `gain =>` applied via `$pi->adc`, plus the PGA **actuated on real silicon**: a fixed PWM-derived input read with `raw()` rises with gain, 322→484→732). Note: actuation asserts direction not a precise ratio (RC-filter source impedance loads the PGA → ~1.5× per step here, and `percent()`/`volts()` still hard-code 4.096 V = V6), and uses `raw()` since that is the only method reflecting the PGA pre-V6. ADS rebuilt + installed 1.03; Changes 1.03 UNREL noted; uncommitted (user commits/releases).
- V6: ADS1115 `volts`/`percent` hard-code 4.096 V FSR vs PGA (`~/repos/rpi-adc-ads` `ADS.xs`) — ✅ 2026-06-22 attempt 1: PASS. Added `pga_fsr()` (reads the PGA index from the config MSB, bits 11-9, and returns its full-scale range per SBAS444), and `volts()`/`percent()` now scale by that FSR instead of a constant 4.096 V (`percent()` keeps its 3.3 V GPIO reference via `ADS_VREF`). Default gain (1, ±4.096 V) output is byte-for-byte unchanged, so `t/140`/`t/45-percent` stay green. Falsified per step-2 (hard-coded 4.096 → RED: gain0=0.640 V vs gain1=0.964 V, 33.6% apart; fix → GREEN: gain0=gain1=0.966 V, 0.2% apart). Tests: V6 block in rpi-wiringpi `t/142-adc_gain.t` (now 52 — same input reads the same volts across gains 0/1 on hardware; raw still rises) + standalone `t/41-volts_gain.t` (PI_TEST-gated, skips gracefully without a usable input). Confirmed the 12-bit (ADS1015 default) path. ADS rebuilt + installed 1.03; Changes 1.03 UNREL noted; uncommitted (user commits/releases).
- V8: `RPi::Const` prereq floor too low (`~/repos/rpi-wiringpi` `Makefile.PL:48`) — ✅ 2026-06-22 attempt 1: PASS. **User made the code change** (floor `1.04` → `1.06` for the `:mcp23017_pins` constants the suite uses; `RPi::GPIOExpander::MCP23017` already requires 1.06). Verified `grep RPi::Const Makefile.PL` shows `1.06`; added the Changes 3.1803 UNREL entry recording the bump. Uncommitted (user commits/releases).
- V10: `bmp()` POD omits its mandatory `$pin_base` param (`~/repos/rpi-wiringpi` `lib/RPi/WiringPi.pm`) — ✅ 2026-06-22 attempt 1: PASS. Documented it: heading is now `=head2 bmp($pin_base)` with a Parameters block (Mandatory, Integer — the BMP180 'pseudo' GPIO pin base, e.g. 100/200, per `RPi::BMP180/new`). Doc-only (the method already forwarded `$_[1]`; `t/340` already passes `BMP_BASE => 100`), so no behavior test. podchecker clean (the lone whitespace warning at `WiringPi.pm:1389` is pre-existing, elsewhere). Changes 3.1803 UNREL noted; uncommitted (user commits/releases).
- V9: stale `RPi::Pin 2.3609` references (`~/repos/rpi-wiringpi`) — ✅ 2026-06-22 attempt 1: PASS. The real prereq floor had moved to `3.1802` (Makefile.PL:59; the audit's `3.1801` was itself stale). Corrected the live refs: the `WiringPi.pm` POD dependency note (`C<2.3609>` → `C<3.1802>`) and the `t/211` comment (dropped the `(2.3609)` annotation). Removed the dead `RPi::Pin` version guard in `t/212` — `if ($RPi::Pin::VERSION < 2.3609)` never fires on a 3.18xx install (`3.1801 > 2.3609` numerically), and the prereq floor already requires a version shipping pin-level `background_interrupt`; rewrote its comment accordingly. **Left the dated `3.1802`-release Changes entries (lines ~117, ~301) untouched** — frozen history: RPi::Pin genuinely was at 2.3609 on 2026-06-12, renumbering to 3.18xx afterward; the old 2.3629/2.3609 entries (RPi::SPI, WiringPi::API, the version header) are likewise historical. Plan verify (`grep 2.3609 lib/ t/`) clean; t/211 (61) and t/212 (48, the background_interrupt hardware path ran and passed on the installed RPi::Pin 3.1801) PASS. Changes 3.1803 UNREL noted; uncommitted (user commits/releases).
- V12: ADS1115 POD/comment typos (`~/repos/rpi-adc-ads` `ADS.pm`) — ✅ 2026-06-22 attempt 1: PASS. Two doc-only fixes: the GAIN AMPLIFIER table listed gain 3 as `±2.024V` (corrected to `±1.024V`), and the differential-MUX code comment for channel 5 showed `0x1000` as `00100000` (corrected to `00010000` — it had duplicated channel 6's `0x2000` binary; the audit's `≈l.46-47` pointer was off-by-one, the real typo was the `0x1000` line). The `%mux` *values* were already correct, so no behavior change and no test warranted. `grep '2.024'` now empty; podchecker clean (the lone whitespace warning at `ADS.pm:684` is pre-existing, in the `register` POD, unrelated). Changes 1.03 UNREL noted; uncommitted (user commits/releases). This clears the ADS dist of its functional + doc audit items (V5/V6/V12); only the discovered V13 (`rate()`) remains.
- V7: MCP42010 `shutdown()` omits the CS toggle (`~/repos/rpi-digipot-mcp4xxxx` `MCP4XXXX.pm`) — ✅ 2026-06-23 attempt 1: PASS. Bracketed the `spiDataRW` in `shutdown()` with `digitalWrite($self->_cs, LOW)` before / `HIGH` after, matching `set()` (l.34-55). Under manual-GPIO CS the part never saw a CS falling edge for the shutdown frame, so the 0x02 command never latched; it now does. `perl -c` clean; framing is byte-for-byte identical to `set()` (same `_bytes`/`spiDataRW`/`_channel`/`_len`). Added behavioral coverage to rpi-wiringpi `t/345-dpot.t` (now 60 tests): for each wiper, set near full-scale and confirm ADC ~98%, then `shutdown()` and confirm the wiper collapses to ~0% (shutdown ties the wiper to B = GND on board 2). **Falsified per step-2**: against a reverted no-CS-toggle copy (loaded via `-I`) the two `shutdown()` subtests go RED (wiper stays ~98% — command never latches), GREEN with the fix; the "before shutdown" checks pass in both. Fixed dist module is installed (user installed it; verified the installed copy carries the CS toggle). Changes 3.1802 UNREL noted; uncommitted (user commits/releases).
- V11: MCP4922 SHUTDOWN BITS POD inverted (`~/repos/rpi-dac-mcp4922` `MCP4922.pm`) — ✅ 2026-06-23 attempt 1: PASS. Doc-only: the bit-12 table read `0 → active (default) / 1 → shut down`, inverted vs DS22250A Reg 5-1 (SHDN: `1 = Active, 0 = Shutdown`) and the XS (`_reg_init`/`_enable_sw` set bit 12 = 1 for active and make active the power-on default; `_disable_sw` clears it for shutdown). Corrected the table to `0 → shut down / 1 → active (default)`; surrounding narrative (active-by-default, hardware /SHDN pin LOW = shutdown) was already correct and untouched. No test added: the SHDN-bit logic is welded into hardware-coupled XS (`_enable_sw`/`_disable_sw` call `_write_dac` doing real GPIO/SPI and return 0, not the register), so there is no pure register-building path to assert polarity against without hardware or a refactor of correct code — out of scope for a doc fix. podchecker clean. Changes 3.1802 UNREL noted; uncommitted (user commits/releases).

Note: V1-V3 were done as one unified, datasheet-grounded refactor of `DS3231.xs` — all BCD setters/getters now route through `setBcdField`/`getBcdField` with masks from the Maxim DS3231 Rev 2; 6/05 register map, so the raw-vs-BCD bug class cannot recur. The XS export block was left untouched; the public per-field API is unchanged.

## Archived Fixes

_None yet._
