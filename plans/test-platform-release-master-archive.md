# Archive — completed V tasks and resolved fixes (test-platform-release-master)

Companion archive for `test-platform-release-master.md`. Completed V tasks are
moved here (one bullet each), resolved Fixes go under Archived Fixes.

## Archived V Tasks

- V1: P0 supersession housekeeping (rewritten 2026-07-09 after the three absorbed plans + two archives were found deleted in commit ecd6971, user confirmed leave-gone) — annotated done/wiringpi-conformance-and-review.md (V24 overtaken by events, 3.1802 shipped 2026-06-12; V47 carried → master V27) and rewrote this plan's Supersession Findings policy to record that the absorbed TCG/DSA ledgers survive only as inline V/B-row descriptions — ✅ 2026-07-09 attempt 1: PASS
- V2: P0 reconcile the absorbed test-platform-regen §9 checklist — source file deleted (V1), §9 read from git ecd6971^; all 10 migration-checklist items verified implemented in-tree (verdict table recorded in the plan under "## V2 verdict"). Deviations noted (facts in board-facts.py not a facts/ dir; schematic PDFs committed as deliverables contra "don't commit"; orchestration via Makefile regen_docs + gen-test-platform.pl + t/04 rather than a standalone regen.py); item 3's stale "t/330" name folds into V3/F12; no new V/B rows needed — ✅ 2026-07-09 attempt 1: PASS
- V24: PC FAQ "RUNNING TESTS" prose audit (was TCG V24) — doc-only, main repo. Fixed the Setup intro's pin-doc pointer (pointed at docs/test-platform/README "for the GPIO pins", but that README explicitly "carries NO pin data" so it can't drift — repointed to docs/test-platform/test-pinout-doc.md, the full pin map, + the test-pinout-detail.jpg image) and dropped the legacy Fritzing breadboard L<> URL (stevieb9.github.io/.../brewbuild_test_platform_bb.jpg) in favour of the in-repo test-pinout-doc.md/detail.jpg (user chose the in-repo reference — no external-URL rot; a relative path also can't be a valid L<> URL anyway). **t/420/421 reconciled as board-2 (user confirmed: they drive the board-2 ADS1015 on real silicon — 421 actuates the PGA, 420 builds $pi->adc the platform way):** added the canonical RPI_BOARD_2 BEGIN convenience block (mirroring t/410) to both, and added them to t/scripts/test_board_2.sh's ordered list (now eight files; also dropped that script's stale "(109/140/325)" pre-renumber sudo-file numbers). The regenerated FAQ table now attributes 420/421 to RPI_BOARD_2. **t/scripts disposition = author-only (recorded):** the rig runners are MANIFEST.SKIP'd (`.sh$`) and drive the author's specific 5-board satellite rig, which a CPAN install lacks - kept author-only and reworded the FAQ so it says they live "in the source repository, not the installed distribution" rather than implying they ship. Prose sweep: RPI_MULTI checked and NOT stale (still read by t/RPiTest.pm). Regen (gen-pod-md.pl) also swept in the V21/V23 mirror rows (310-sysinfo_unit, 534-rtc_unit) that were never regenerated into the table; shortened t/534's TESTDOC to the sibling "RPi::RTC::DS3231 unit (HW-free)" form so its long description didn't balloon the whole table's column width (220-line churn -> 18). Verified: t/420/421 perl -c exit 0, test_board_2.sh bash -n OK, t/01/t/03/t/04 enforcement PASS, t/910 POD syntax PASS (t/905 link-check skipped - optional Test::Pod::LinkCheck absent; no L<> links added, one external URL removed). **Uncommitted in ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V23: PC RPi::RTC::DS3231 depth (was TCG V8) — XS dist 0.04 UNREL, installed 0.04. **BCD-round-trip item was already done:** main t/532-rtc-bcd.t already carried the exhaustive 0..99 valid-BCD + round-trip, illegal-BCD 10-15, named callouts and 0..9 identity. Filled the genuine gaps (pure coverage, no defect): (1) new dist t/75-validation.t (26 tests, HW-free — bare-blessed object, no fd) covering _write_time()'s six field-range croaks (year 2000-2099 / month 1-12 / mday 1-31 / hour 0-23 / min 0-59 / sec 0-59, all validated BEFORE the fd burst-read, driven via out-of-range date_time() strings — note the captures are zero-padded so the croak reads "Month (00)" etc.), the date_time() format croak (ungated, unlike the RPI_RTC-gated t/70), and the am_pm()/clock_hours() bad-arg croaks; plus temp()/temp('f') sign + two-decimal format + Fahrenheit conversion over known Celsius incl. the **negative path** (getTemp stubbed — the XS sign decode is HW-only and t/50-temp.t can't reach sub-zero; -25C→-13.00F proves the old +231 sign bug stays fixed). (2) extended main t/532 with explicit named field-max BCD assertions (sec/min 59→0x59, hour 23→0x23, mday 31→0x31, month 12→0x12, year 99→0x99, wday 7→0x07). (3) added main mirror t/534-rtc_unit.t (TESTDOC; the validation + temp-negative coverage vs the installed module). dist make test 35 PASS; main t/532+t/534 261 PASS; t/01/t/03/t/04 enforcement PASS; MANIFEST clean (dist + main). **HW half stays queued on board-4 (V8):** t/530-rtc.t drives the real chip once board-4 is live. **Uncommitted in ~/repos/rpi-rtc-ds3231 + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V22: PC WiringPi::API interrupt machinery (was TCG V23) — XS dist 3.1804 UNREL. **Row was largely stale:** the machinery is already covered — t/75 (set_interrupt validation, dispatch routing/drain via faked self-pipe + stubbed _arm_interrupt, interrupt_buffer, run_interrupt_loop, background_interrupts spec validation, auto_dispatch enable/disable/unknown-signal), t/76 (pipe/fcntl failure branches), t/77 (plural control channel), t/87 (handle waitpid errno), t/88 (results/shared framing via worker). Filled the two genuine gaps: (1) extended t/75 — auto_dispatch_interrupts() now proves a **pre-existing** handler on the chosen signal is saved on enable and **restored** (not deleted) on disable (the prior block only covered the no-prior-handler delete path); (2) new t/78-background_interrupt_single.t (TESTDOC, 9 tests) for the **singular** background_interrupt() — pre-fork arg-validation croaks (pin/edge/callback/debounce, no fork) + the results channel (B5) round-trip: a real fork with set_interrupt()/wait_interrupts()/wiringPiISRStop() stubbed (inherited across fork, no GPIO), child frames a return up the pipe, parent handle read() drains it length-framed intact; alarm(10) watchdog + guaranteed stop/reap so it can never hang; plus the no-results-channel fh()/read()==undef case. **No defect found (pure coverage).** **Also fixed a pre-existing MANIFEST bug surfaced by the verify step:** removed a stale `INT.md` entry (never a tracked file; the doc is docs/interrupt-examples.md) that was failing manicheck / would break `make dist`. Full dist make test 196 PASS (34 files); MANIFEST clean. Dist-only (no main mirror — fork/pipe machinery; main's t/117 is a lighter unit). **Uncommitted in ~/repos/wiringpi-api + ~/repos/rpi-wiringpi (plan) — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V21: PC RPi::SysInfo HW-free (was TCG V22) — XS dist 1.03 UNREL, already richly tested (11 files + Pi 3/4/5 fixture-replay via the _run/_slurp seams). **Fixed TCG F15 (two defects), user chose return-'' over croak:** (a) `_format()` passed the XS `-1.0` failure sentinel from cpuPercent()/memPercent() straight through as "-1.00" — it now returns '' on any negative, so cpu_percent()/mem_percent() report '' on failure (matching core_temp()'s empty-on-error convention); (b) raspi_config()'s config.txt filter used `grep -E -v '^\s*(#|^$)'` — the inner `^` made the blank-line branch unreachable; corrected to `(#|$)` so blank/whitespace-only lines are skipped alongside comments. Ungated t/05-cpu_percent.t + t/10-mem_percent.t (was RPI_BOARD; cpuPercent/memPercent read /proc so they run on any Linux → now gated `$^O eq 'linux'`). Extended dist t/50-helpers.t with the -1.0 sentinel (unit + cpu/mem functional & OO via stubbed XS), the raspi_config grep-regex fix (command-capture + real-grep behavioural), and a 16GB Pi 5 revision decode (e04170, mem-field 6). Added main mirror t/310-sysinfo_unit.t (37 tests; TESTDOC; MANIFEST; installed 1.03 so F15 assertions are live not dist-only). dist make test 270 PASS; mirror + t/01/t/03/t/04 enforcement PASS; MANIFEST clean. **Uncommitted in ~/repos/rpi-sysinfo + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V20: PC RPi::OLED::SSD1306 HW-free + live coverage (was TCG V21) — XS dist 3.1802, no functional tests. **Fixed TCG F16 (option b):** new() is a documented singleton but silently ignored a mismatched i2c address/splash on a second call; it now warns (still returns the live display) and stores i2c_addr/splash on the instance to detect the mismatch. Added dist t/05-unit.t (HW-free, bless bare object: text_size/rect(4 bounds)/pixel/dim/invert_display croaks — the row's "6 validating methods" is 5 — plus singleton returns-same + F16 warn via stubbed XS) + dist t/10-live.t (**LIVE on-device, gated RPI_OLED**: new/clear/text_size/string/rect/pixel/display/invert/dim — 12/12 PASS on the real panel) + main mirror t/510-oled_unit.t (19 tests; TESTDOC; FAQ regenerated; F16-warn assertion dist-only since installed is 3.1801). make test + author + mirror + live all PASS; MANIFEST clean. Also fixed pixel()'s Y-bound croak typo ("betwen"→"between", user-requested); dist test tightened to the corrected message, main mirror keeps a broad regex (installed 3.1801 still has the typo). **Uncommitted in ~/repos/rpi-oled-ssd1306 + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V19: PC RPi::LCD HW-free coverage (was TCG V20) — pure Perl 3.1802, no functional tests. Folded in init() die→confess (user's call; matches the module's Carp style). Added dist t/05-unit.t + main mirror t/621-lcd_unit.t (20 tests; TESTDOC; FAQ regenerated): init()'s required-key deaths (the row said "14" — it's actually **13**: rows/cols/bits/rs/strb/d0-d7, each covered), valid init() via a mocked inherited lcd_init() returning + storing the fd, _fd(-1) confess at the 8-LCD limit + set/get round-trip, and print/puts + print_char/put_char alias identity (glob-alias coderef equality) — all HW-free (bless bare object; no DESTROY). make test + author + mirror PASS; MANIFEST clean. **Uncommitted in ~/repos/rpi-lcd + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V18: PC RPi::BMP180 _pin_base validation (was TCG V19) — pure Perl 3.1802, no functional tests. Changed _pin_base die→croak (user's call; added use Carp). Added dist t/05-unit.t + main mirror t/533-bmp180_unit.t (9 tests; TESTDOC; FAQ regenerated): _pin_base non-integer/unset croaks, valid set/get, new()-no-pin croak — all HW-free (bless bare object; module has no DESTROY). make test + author + mirror PASS; MANIFEST clean. **B9 corrected:** traced the OSS/compensation math on user's "should we fix WiringPi::API now?" question — it's NOT in WiringPi::API (bmp180Temp/Pressure are `return analogRead(pin)` passthroughs) but in EXTERNAL wiringPi devLib bmp180.c (oss hardcoded 0). Recommended NO fix now (nothing to fix there, no defect; clean path = reimplement in RPi::BMP180 via RPi::I2C post-release). **Uncommitted in ~/repos/rpi-bmp180 + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V17: PC RPi::DHT11 noboard-mode coverage (was TCG V18) — XS dist 1.06, not reworked. **Fixed the noboard gap:** c_cleanup() now honors RDE_NOBOARD_TEST (setup() and the temp/humidity reads already did — I checked, setup() was already handled), so the whole lifecycle runs without wiringPi in noboard mode. **Reconciled TCG F17:** the HW-free tests were gated behind RPI_BOARD (t/00) / RPI_DHT11 (t/05,t/10) so they never ran; now t/00-load, t/05-noboard, t/10-bad_params run unconditionally via RDE_NOBOARD_TEST (real new() API), RPI_DHT11 reserved for live-sensor tests, and the documented-but-unused RDE_HAS_BOARD removed from the POD. Added temp('f')=32 (C→F) coverage. make test 12 PASS (all HW-free tests now RUN, not skip); podchecker OK; MANIFEST clean. Tests dist-only (user confirmed — DHT11 isn't a board device). **Uncommitted in ~/repos/rpi-dht11 — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V16: PC RPi::HCSR04 (was TCG V17) — XS dist 3.1802, not reworked. **Fixed TCG F11:** new() read the class name as the invocant and (via the BEGIN block's no-strict-refs) wrote pins into a global package hash, returning the class-name string — now blesses and returns a real per-instance object. **Fixed TCG F12:** the trig/echo pin guard `$p < 0 && $p > 40` was always false (dead code) → now `||` (rejects <0 or >40). Errors changed die→croak (user's call; added use Carp). Rewrote t/05-new.t HW-free (stub XS _setup): covers the blessing, per-instance state, missing-param dies, and pin-range croaks — no longer gated on RPI_BOARD/RPI_HCSR04. cm/inch math extraction deferred to B7 (not done here, per the row). make test + author tests PASS; the real-sensor 10/15/20-*.t stay hardware-gated. No main mirror (HCSR04 isn't a board device). NOTE: the Makefile.PL BuildCheck guard initially blocked a bare `perl Makefile.PL` because RPi::Const::BuildCheck (the new V25 submodule) wasn't installed — only base RPi::Const 1.07 was; a bare Makefile.PL doesn't resolve CONFIGURE_REQUIRES. RESOLVED: user installed the local rpi-const (bundling BuildCheck), so plain builds now pass the wiringPi check without RPI_DIST_RELEASE. **Uncommitted in ~/repos/rpi-hcsr04 — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V15: PC RPi::StepperMotor HW-free coverage (was TCG V16) — pure Perl 3.1802, no functional tests (not reworked, so the row was accurate). **Fixed TCG F10:** speed() validation used an inverted grep (`$speed ne $_`) that was always true, so the croak was dead code and any speed passed silently; changed to `eq` so invalid speeds now croak. Added dist t/05-unit.t (mock-expander-driven, delay=0: cw/ccw step patterns + counter wrap for full/half via STEPPER_SEQUENCE reconstruction, _turns() 64:1 gearing + round-half-up, and new()/pins/direction/speed validation croaks). Mirrored the version-stable subset into main t/352-steppermotor_unit.t (14 tests; TESTDOC; FAQ regenerated) — the F10 speed-rejection assertion is dist-only (installed is 3.1801, predates the fix). Dist make test + author tests PASS; mirror PASS; MANIFEST clean. **Uncommitted in ~/repos/rpi-steppermotor + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V14: PC RPi::Serial HW-free coverage (was TCG V15) — XS dist (3.03 UNREL, crc16.c POLY 0x8408), had zero functional tests. **TCG F14 fixed:** new() now validates device/baud and croaks on tty_open<0 (was: returned an fd==-1 object); close()/DESTROY() guard against an invalid fd. **Also fixed 3 more bugs the coverage surfaced:** (1) write() silently wrapped >255 via pack("C") → now croaks 0-255 (user's call); (2) **tx() was broken** — it fed frame characters to write() (which packs an integer), so every non-digit char went out as 0x00 → fixed to putc() the frame chars, write() only the 2 integer CRC bytes; (3) **rx() returned a defined 0** while mid-assembly (fell off the sub end = last if-condition) → now returns explicit undef; (4) **flush() called an undefined tty_flush** → added tty_flush() XSUB (tcflush TCIOFLUSH). Tests: dist t/05-unit.t (HW-free: crc vectors vs pure-Perl ref, new()/write() validation, tx() frame order, rx() reassembly/discard/mid-frame-undef/CRC-mismatch, flush() — 28 tests) + dist t/10-loopback.t (**LIVE TX-wired-to-RX round-trip**, gated RPI_SERIAL_LOOPBACK: byte, string, 3 CRC frames, flush — 8/8 PASS on /dev/ttyAMA0). Main mirror t/438-serial_unit.t = crc vectors only (installed was 3.02, predates + broken for the framing fixes). make test + author tests PASS; FAQ regenerated; MANIFEST clean. **FOLLOW-ON (user-directed, same day):** rewrote main t/610-serial.t into a full live-loopback suite (26 serial assertions: all-256-byte putc/getc sweep, write() integer interface + croaks, puts/gets short-read, crc vectors, avail/flush, and a real-world **CRC-framed messaging example** with corruption-detection) — which surfaced + fixed a **6th bug**: tty_open() wasn't fully raw (ICRNL silently mapped input 0x0D→0x0A, so one byte never round-tripped). User authorized installing RPi::Serial 3.03 into site_perl; main dist prereq bumped 3.02→3.03 (Makefile.PL). t/610 now 26/26 serial tests PASS on the loopback (only the pre-existing pin-8 cleanup contamination fails — see mcp3008-cs-pin-alt31 memory). **Uncommitted in ~/repos/rpi-serial + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V13: PC RPi::SPI HW-free coverage (was TCG V14) — RPi::SPI was reworked too (pure Perl 3.1802: SPI_NO_CS + bit-bang), had zero functional tests. **TCG F9 moot everywhere:** RPi::SPI uses `wiringPiSPISetup(...) < 0 -> die`, never spi_setup/wpi_setup(exit) (those were dropped). **TCG F13 fixed** (user chose croak): `_speed` now croaks on an explicit non-positive/non-integer speed (undef still = default 1MHz) instead of silently rewriting 0->1MHz. Added minimal rw() arg validation (croak on non-arrayref buf / non-positive-int len). Added dist t/05-unit.t (routing, _cs, _speed+F13, bit-bang validation, rw() arg validation, rw() framing for plain/GPIO-CS±SPI_NO_CS/bit-bang via stubbed transport). Mirrored the version-stable subset into main t/437-spi_unit.t (18 tests; TESTDOC; FAQ regenerated) — the F13-croak + rw-validation asserts are dist-only until the fixed RPi::SPI is installed/released (installed is 3.1802-rework, lacks my two fixes). Dist make test + author tests PASS; mirror PASS; MANIFEST clean. **Uncommitted in ~/repos/rpi-spi + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V12: PC RPi::ADC::MCP3008 HW-free coverage (was TCG V13) — the dist was heavily reworked in the last few days (3.1802 UNREL), overtaking most of V12: XS removed (SPI delegated to RPi::SPI) so **TCG F9 (spi_setup/wpi_setup exit→croak) is moot here** — those setup calls were already replaced in 3.1801, then the layer removed; GPIO-CS CE0-strobe FIXME resolved (RPi::SPI SPI_NO_CS); raw()/percent() channel-validation croaks already shipped + already covered by the dist's t/05-input-validation.t. Remaining gap filled: added HW-free read-path coverage (10-bit decode, percent ÷1023, per-channel frame build) by stubbing the _spi seam — dist t/06-fetch-decode.t (12 tests) + mirrored into main t/436-mcp3008_unit.t (20 tests, incl. validation; TESTDOC; ungated; FAQ regenerated). User chose seam-stub only (no _fetch refactor — decode extraction stays B6). Dist make test + author tests PASS; mirror PASS; MANIFEST clean. F9 re-homed to V13 (RPi::SPI). **Uncommitted in ~/repos/rpi-adc-mcp3008 + ~/repos/rpi-wiringpi — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V10: PH board-5 finish — CLOSED by user directive 2026-07-09 ("that board is completed"). Board-5 is blessed (re-blessed V34), gerbers plotted, ordered; F6 resolved (LCD1 = HD44780_20x4). Canonical firmware = docs/sketch/arduino.ino (V33), and V33's edits were behavior-neutral (comment/header/newline only), so a rig already flashed from docs/sketch is running canonical behavior — no re-flash needed. Deliberately NOT done (user/frozen-board policy): sch-rev stamp (board-5 sch UNSET vs PCB 1 — accepted, see B22) and the 4 REF** mounting-hole annotations. Live t/605 rig-run stays in V28. — ✅ 2026-07-09 attempt 1: PASS (closed by user; board complete)
- V33: PH canonical Arduino 0x04 sketch (F11) — read-only protocol trace of t/605 + all 9 rpi-i2c examples proved docs/sketch/arduino.ino is canonical (big-endian word reads + correct write round-trips; the rpi-i2c fork was a broken refactor that couldn't pass write_block round-trip). De-duped this repo to the single flat path docs/sketch/arduino.ino (deleted nested docs/sketch/arduino/arduino.ino + MANIFEST:47); corrected its "little"→"big" endian comments + header path; overwrote ~/repos/rpi-i2c/examples/arduino.ino with the canonical body (RPi::I2C header) — bodies now byte-identical (one firmware, two headers). MANIFEST clean. Correction to F11: the rpi-i2c fork WAS shipped (MANIFEST:3). Rig-flash left to V10/V28. **Uncommitted in both ~/repos/rpi-wiringpi and ~/repos/rpi-i2c — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V9: PH lock unification mechanism (F3; DSA B5/F17) — made board-locks.json the single "off-limits" source. Added `check-board-locks.py --names` (machine-readable locked-board names + USAGE doc); t/04 now DERIVES %FROZEN from `--names` instead of a hardcoded hash (so a frozen-but-unblessed board is impossible by construction); gen-kicad.py refuses (exit 1) to scaffold any board present in board-locks.json, ahead of its existing refuse-if-exists. Verified: t/04 6/6; --names lists boards 2-5; gen-kicad refuses board-2 with a lock-specific message writing nothing (git clean); non-locked name still scaffolds. No board design files touched. — ✅ 2026-07-09 attempt 1: PASS
- V7: PH rpi-eeprom-at24c32 1.00 residue (TCG B5) — in ~/repos/rpi-eeprom-at24c32: deleted dead static _writeBlock() (backed the already-removed eeprom_write_block); deleted the orphaned eeprom_close()/eeprom_read_current_byte() XS exports the OO layer never used (user chose "delete both") — both C bodies + their MODULE decls. Added HW-free tests t/10-api_and_validation.t (20 tests: removed-symbol assertions + address/byte validator croaks + boundaries); Changes 1.00 entry + MANIFEST updated. Build OK; make test 21 PASS; RELEASE_TESTING author tests 24 PASS (manifest + pod-coverage clean). Silicon verification of the 1.00 ACK-polling write cycle split out to V35 (board-4 hardware). **Uncommitted in ~/repos/rpi-eeprom-at24c32 — user commits.** — ✅ 2026-07-09 attempt 1: PASS
- V5: PH ADS1115/ADS1015 naming reconciliation (F7) — physical part is 12-bit ADS1015 (HW-confirmed 2026-06-23); most of the correction shipped that day, V5 cleared the last residual miss: t/421-adc_gain.t TESTDOC ADS1115→ADS1015 + regenerated FAQ (gen-pod-md.pl → lib/RPi/WiringPi/FAQ.pod + docs/pod/FAQ.md, 2 lines). t/ and lib/ now 100% ADS1015. Per user decision, the 4 board-mirror helpers (board-model.py, model-from-tests.py, datasheet-pinouts.json, gen-pinout-images.py) + frozen board-2 KiCad KEEP "ADS1115" (module-as-placed) — deliberate docs/board split, not literally one name everywhere. t/04 6/6 PASS — ✅ 2026-07-09 attempt 1: PASS
- V4: PH board-2 provenance (F4) — CLOSED BY RESCOPE (user directive 2026-07-09: user owns all board-2 symbols/footprints, no modifications/restores; board2 frozen). Read-only verify established board-2 opens/renders/fabs from a fresh clone (sch embeds lib_symbols, pcb embeds placed-footprint geometry); only external ref is the user's own RPi: ADS1115 symbol+footprint via global config. All original modification items dropped (no restore, sym-lib-table, footprint migration, .pretty refresh, sch-rev stamp, or re-bless). Sch-vs-PCB rev lockstep left to B22. F4 marked VALIDATED-with-correction in place — ✅ 2026-07-09 attempt 1: PASS (rescoped)
- V34: PH re-bless boards 4 & 5 (F14) — user-authorized re-bless run in-session; `check-board-locks.py --bless 4 5` re-snapshotted board-4 (12 files) + board-5 (13 files), reconciling the hashes that had lagged since 0011bce. Diff scope confirmed: only 5 hash lines changed (board-4 pcb/pro/sch, board-5 pcb/pro); boards 2/3 byte-untouched. `check-board-locks.py` now clean for all 4 boards; **t/04 6/6 PASS**. board-locks.json modified in working tree — **commit pending user** (git commits are user-only). B22 sch-rev stamping deliberately NOT bundled (edits frozen boards; separate task) — the bless captured board-5's UNSET sch rev as-is — ✅ 2026-07-09 attempt 1: PASS (commit pending user)
- V8: PH board-4 PCB completion — verified complete on reconcile: PCB+SCH both title-block rev 2 (lockstep), RPI_BOARD_4 silk present, Edge.Cuts drawn, user ordered fab. **CORRECTION (2026-07-09, later same day): board-4 has a board-locks.json ENTRY but its hashes are STALE — the board was finalized in 0011bce (2026-07-07) after the last bless (ab80ad0, 2026-07-05), so check-board-locks.py fails for it and t/04 test 4 fails on HEAD. The lock half is NOT closed; tracked as F14/V34 (user re-bless). Layout/silk/rev/order work stands.** [F14/V34 RESOLVED 2026-07-09: board-4 re-blessed, lock now clean — board-locks.json commit pending user.] (Residue: title-block *title* text is blank on board-4 — cosmetic.) — ✅ 2026-07-09 attempt 1: PASS (with V34 lock caveat)
- V3: P0 fix stale references — removed the misleading `legacy/` refs (t/04:45, test-pinout-doc.tmpl.md; legacy/ is a stray untracked .kicad_prl, not a board project) and fixed the board range 1..4→1..5; board-5 status was already consistent ("finalized & ordered" in matrix + README — the plan's "normalize to in-progress" premise was itself stale, board-5 IS finalized). F12 pre-renumber sweep: fixed t/543:10 (t/420-422→t/540-542), and doc prose t/100,104,106,409→309 + sysinfo t/400-408→t/300-308; regenerated test-pinout-doc.md via render-doc.py; full compact-citation sweep across t/ + docs now clean. perl -c OK. (t/04 does NOT fully pass, but only due to the pre-existing board-lock drift filed as F14/V34 — not a V3 regression.) — ✅ 2026-07-09 attempt 1: PASS
- V25: PR complete the wiringpi-version-single-source plan (BuildCheck + xt drift gate + docs) — **CLOSED as verify-only; the master's "residual work" description was stale.** Re-verified against the current tree: `scripts/audit-family-buildcheck.pl --markdown` reports ZERO drift (9 converted BuildCheck-shim, 11 pure-perl n/a, 3 standard-C XS needing no guard); the shim is present in all dists that need it. rpi-const finalized — `RPi/Const/BuildCheck.pm` present, `WIRINGPI_MIN_VERSION` ('3.18') shipped in the `:wiringpi` tag, dist at 1.07 UNREL, `t/55-buildcheck.t` + `t/50-const_coverage.t` = 221 PASS. Both xt drift gates green — `xt/author/buildcheck-audit.t` + `xt/author/min-version-doc-sync.t` = 17 PASS. Docs single-sourced (B2 gen-min-version.pl regen + sync gate; F6 resolved). Sub-plan `wiringpi-version-single-source.md` already recorded V1-V8 + B1-B6 all complete — that view is the accurate one. B4 confirmed on this system: Dist::Mgr repo ~/repos/dist-mgr at 1.17, installed 1.17 matches, `add_buildcheck` present. No code changes; no commits (releases are user-only, deferred to V29). — ✅ 2026-07-09 attempt 1: PASS

- V26: PR version/floor/Changes AUDIT (F1/F2/F9) — re-derived every floor + claimed defect from the CURRENT tree (staleness assumed). **No floor edit required:** all 23 main-repo PREREQ_PM floors resolve at their release wave (leaves wave-3 before main wave-4), and every fix-carrier floor is already raised — RPi::Pin 3.1802 (Pi-5 pwm() fix → t/400/t/405), RPi::Serial 3.03 (V14), RPi::RTC::DS3231 0.03 (BCD/temp), RPi::Const 1.06 (:mcp23017_pins). EEPROM::AT24C32 floor evaluated for a 0.01→1.00 raise and DELIBERATELY LEFT at 0.01 — main t/540-543 only exercise _check_addr/_check_byte (present in 0.01), no removed-symbol/ACK-poll dependency. **F9 fixed:** rewrote 9 stale subject-of-work filenames in main's 3.1803 UNREL Changes (t/321→532, t/320→530, t/140→405, t/109→400, t/141→420, t/142→421, t/340→531); left line-79's t/330 (describes the old ref it corrected) + the foreign dist t/55-samples.t + bare-number t/211/t/212 (never stale). **F2 already resolved** by the user's rpi-i2c rework (header now 3.1803 UNREL, zero 2.3609 residue) — validated, no edit. **F1 validated w/ correction:** no longer a blocker (rpi-pin module now 3.1803, 3.1802 dated 2026-07-05); floor correct, CPAN-index confirm deferred to V32. Audit table recorded in the plan under "## V26 audit". Conservative scope held — zero edits outside rpi-wiringpi; rpi-pin's empty 3.1803 UNREL stub flagged as a V31 item. `perl -c Makefile.PL` OK; UNREL section clean of stale refs. **Uncommitted in ~/repos/rpi-wiringpi (Changes + plans) — user commits.** — ✅ 2026-07-09 attempt 1: PASS

- V27: PR t/425 servo calibration windows (was conformance V47; USER powered board-2 rig, no servo attached — the ADS1015 reads the GPIO18 PWM line directly on A0). Replaced the vacuous ±40 (MAX_IN) bound with a real, empirically-calibrated gate. Key finding: because the ADS integration window (~8ms) is shorter than the 50Hz PWM period (20ms), a SINGLE feedback read is phase-dependent (swings 0..~33 at every servo position — useless as a gate, since a severed line reading 0 sits inside every raw span), but the MEAN of 100 reads cancels the phase noise and tracks ideal duty (pwm/2000*100) to within ~0.3pt: LEFT(60)→~3.0, CENTRE(150)→~7.5, RIGHT(255)→~12.75 (measured over 3 runs, stable ±1pt). Single-sourced in RPiTest.pm: `%servo_adc_windows{pi5}` = {60→[1.5,5.5], 150→[5.5,10.5], 255→[10.5,15.5]}, plus `rpi_servo_adc_window()` (calibrated table + ideal-duty fallback for uncalibrated boards) and `rpi_servo_adc_mean($adc,$ch)` (N=100 @ 0.01s), both exported. t/425 rewritten: asserts mean-in-window at the 3 static positions + a sweep gate (every read ≤ SWEEP_CEIL=36 catches stuck-high; sweep-peak > SWEEP_MIN_PEAK=15 catches stuck-low/severed line). Proven to FAIL on a broken path (means collapse to ~0 → out of every window; peak → ~0 → fails >15). Live run: 6/6 calibration + sweep-peak GREEN; the final run's RIGHT mean landed at 14.27, validating the 15.5 ceiling (a tighter [10.5,14] would have false-failed). Same powered session LIVE-PROVED t/400-pwm_hw_mods.t + t/405-pwm_i2c_adc.t — all their PWM→ADC window assertions pass on this rig. **Lone remaining failure in t/425/t/400/t/405 run-in-isolation is the pre-existing pin-18 mode-31 contamination (got 0/input, expected 31/RP1-null; wiringPi releases a driven PWM pin to input(0), not boot-pristine null(31)) — PROVEN pre-existing via stash-compare (stashed original t/425 fails the identical 2 subtests, got 0/expected 31) — flagged for V28 full-suite (test_board_2.sh) verification.** **Uncommitted in ~/repos/rpi-wiringpi (t/RPiTest.pm + t/425-servo.t) — user commits.** — ✅ 2026-07-10 attempt 1: PASS

- V35: PH rpi-eeprom-at24c32 on-silicon verification (USER hardware; DS3231/AT24C32 ZS-042 module wired to the I2C bus, board-2 declared disconnected). `i2cdetect -y 1` confirmed 0x57 (AT24C32) + 0x68 (DS3231) present. `RPI_BOARD=1 RPI_EEPROM=1 prove -lv t/540-eeprom_args.t t/541-eeprom_read_write_byte_croak.t t/542-eeprom_read_write_byte.t` = **166/166 PASS**. t/542 write→read round-trips on real silicon — single address (100→232) + the top page (addrs 4080-4095 ← vals 100-115) — confirm the 1.00 **ACK-polling write cycle** returns as soon as the AT24C32 ACKs (not a fixed sleep). Gated as EEPROM-only (RPI_BOARD+RPI_EEPROM, NOT RPI_BOARD_4) since only the RTC/EEPROM module is wired — BMP180 (0x77) + OLED (0x3c) absent per i2cdetect. All pin-cleanup subtests green (no pin-18 mode-31 contamination this isolated run). **NOTE: 0x48 (board-2 ADS) still ACKs on the bus despite board-2 being declared disconnected — flagged to user; does not affect the EEPROM-gated result.** — ✅ 2026-07-10 attempt 1: PASS

- V37: PR board-INDEPENDENT pre-release verification (split from V28 2026-07-10; fabbed boards not yet delivered). (a) rpi-wiringpi board-independent suite GREEN — 104 files / 738 tests, all board-gated files correctly skip (no RPI_BOARD). (b) Family disttest/build+test sweep: **all 26 dists PASS** (wiringpi-api + 25 rpi-* incl. the 3 sensor leaves accel/gyro/radar, pca9685, both steppermotors). (c) MANIFEST fullcheck clean EXCEPT 2 real gaps → **V38**: `t/440-pca9685.t` (rpi-wiringpi) + `t/02-validation.t` (rpi-gpioexpander-mcp23017) are committed test files absent from MANIFEST (won't ship); the other "extras" are non-shipping dev files (docs/, xt/author/, pics, stl). (d) Worktree clean after the sweep — make artifacts are gitignored everywhere except rpi-sysinfo, whose generated `SysInfo.c` leaked untracked → cleaned via `make realclean`; only the 2 intentional plan-file edits remain. **Fix 1** unblocked step (a): installed RPi::I2C was 3.1802 (pre-rework) while `t/250-i2c_unit.t` asserts the local **3.1803** API — built+installed 3.1803, suite green; STRATEGIC B15/rpi-i2c coupling flagged for the user (Discovery Tracking, unresolved). — ✅ 2026-07-10 attempt 1: PASS

## Archived Fixes

_None yet._
