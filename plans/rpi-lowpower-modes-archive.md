# Archive — completed V tasks and resolved fixes

## Archived V Tasks

- V1: RPi::StepperMotor `off()` (de-energize all coil pins LOW, pins stay OUTPUT, returns 0) + `cleanup()` refactored to call `off()` (fixes expander-path de-energize gap); POD `=head2 off`, cleanup POD, end-of-SYNOPSIS "Powering off" block, Changes entry; dist t/05-unit.t off()/cleanup() assertions + guarded t/352 mirror + explicit t/350 teardown park — ✅ 2026-07-16 attempt 1: PASS (dist 20 tests, t/352 17 tests with off block SKIPped pre-install)
- V2: RPi::OLED::SSD1306::128_64 `sleep()` (charge pump off 0x8D 0x10, display off 0xAE) and `wake()` (0x8D 0x14, 0xAF) via the existing `ssd1306_command` XS binding (no XS recompile); SSD1306_* command constants, POD `=head2 sleep`/`=head2 wake`, end-of-SYNOPSIS "Powering down" block, Changes entry; dist t/05-unit.t HW-free command-sequence assertions (stub ssd1306_command) + guarded t/510 mirror + t/520-oled_cleanup.t teardown park — ✅ 2026-07-16 attempt 1: PASS (dist 26 tests, t/510 23 tests with sleep block SKIPped pre-install). Note: plan's V2 verify command named t/510 as the dist test; the dist HW-free test is actually t/05-unit.t (t/510 is the rpi-wiringpi mirror).
- V3: RPi::TFT::ST7735S `sleep()` (SLPIN 0x10 + ~120ms nap) and `wake()` (SLPOUT 0x11 + ~120ms nap) — the deep driver/booster power-down, distinct from the existing DISPOFF/DISPON `off()`/`on()`; POD `=head2 sleep`/`=head2 wake`, end-of-SYNOPSIS "Powering down" block, Changes entry; dist t/06-draw.t SLPIN/SLPOUT command assertions (stub _nap, Fake::SPI) + guarded t/448 mirror + t/447 `$cleanup` closure parks the panel (`off()` then guarded `sleep()`) — ✅ 2026-07-16 attempt 1: PASS (dist 57 tests, t/448 79 tests with sleep block SKIPped pre-install). Resolves F3 (method gap) and F10 (teardown). Pre-existing POD link warnings (7, for signatured =head2 headers like color565/char) left as-is — not introduced by V3.
- V4: RPi::ADC::ADS conversion-mode/power coverage. No driver change. Added a "Conversion mode / power" block to the SYNOPSIS (mode 1 = single-shot / powers down between reads = default; mode 0 = continuous) + Changes entry; extended the existing rpi-wiringpi t/422-adc_unit.t with MODE-bit (config bit 8, 0x100) assertions: new() default is single-shot (bit set), mode(0) clears it, mode(1) restores it — ✅ 2026-07-16 attempt 1: PASS (t/422 16 tests, dist t/920 12 tests). Resolves F4; F13 VALIDATED (no change — the chip is already left single-shot/powered-down). Corrections to the plan: register()/bits() return CACHED config (not a live I2C read), so the assertion is HW-free/unit-level and needs no install-lag guard (mode() long-shipped); the existing t/422-adc_unit.t (not a new file) was the home, so no MANIFEST change.

- V5: RPi::DAC::MCP4922 software-shutdown coverage, rpi-wiringpi only (no dist change — SYNOPSIS already documents disable_sw/enable_sw/SHDN). Extended t/410-dac.t: for DAC 0 and 1, set() full-scale (ADC ~full), disable_sw($n) (ADC ~0, the power-down), enable_sw($n)+set() (ADC ~full); teardown shuts both DACs down via disable_sw(0)/disable_sw(1) before cleanup. disable_hw not tested (t/410's DAC has no SHDN pin; croak covered in t/411). Resolves F5 + F11 — ⚠️ 2026-07-16: implemented + compiles; installed MCP4922 has the methods; NOT executed this session (hardware-gated, board-2 DAC↔ADC). Closed at the user's request without a run — confirm with `prove t/410` on the Pi and handle any fallout then.

- V6: RPi::Gyro::MPU6050 teardown — added `$mpu->sleep` to the `$cleanup` closure in t/358-gyro.t (before `close`), leaving the IMU asleep (~10uA) however the file exits; added a "Powering down" sleep()/wake() block to the MPU6050 SYNOPSIS + Changes entry — ✅ 2026-07-16 attempt 1: compiles; teardown call runs on board (RPI_MPU6050), and sleep() is already exercised in the test body. Resolves F8.
- V7: RPi::StepperMotor::A4988 teardown — added `$motor->sleep` to the `$cleanup` closure in t/353-a4988.t (before `$motor->cleanup`), driving SLEEP low to power the driver down; added a "Powering down" disable()/sleep()/wake() block to the A4988 SYNOPSIS + Changes entry — ✅ 2026-07-16 attempt 1: compiles; teardown call runs on board (RPI_A4988), sleep() already exercised in the test body. Resolves F9.
- V8: RPi::DigiPot::MCP4XXXX teardown — added `$pot->shutdown(POT0)`/`$pot->shutdown(POT1)` before `$pi->cleanup` in t/445-dpot.t, parking both wipers in the A-terminal-open low-power state (the test otherwise `set(0)`s them back out of shutdown). No dist change (SYNOPSIS already documents shutdown) — ✅ 2026-07-16 attempt 1: compiles; teardown call runs on board (RPI_MCP4XXXX), shutdown() already exercised in the test body. Resolves F12.

- V9: RPi::RTC::DS3231 (promoted from B1) — exposed the two battery/power bits: `en_32khz($bool)` (Status 0x0F bit 3) and `osc_on_battery($bool)` (Control 0x0E bit 7 EOSC, positive-sense so the safe state is the default; loud POD/SYNOPSIS warning that `osc_on_battery(0)` freezes the clock on power loss + sets OSF). Pure Perl over the already-exported `enableRegisterBit`/`disableRegisterBit`/`getRegisterBit` XS — the previewed "new setRegister XS primitive" turned out unnecessary (the bit helpers were already exported), so **no XS change / no recompile-for-functionality**. Added `=head2` POD for both under a new "Power / battery methods" section, a "Battery / power" SYNOPSIS block, a Changes entry, MANIFEST += t/80-power.t. Tests: dist t/80-power.t (HW-free — stub _fd + the 3 bit helpers, assert each method drives the right register/bit and the EOSC inversion) + guarded block in rpi-wiringpi t/530-rtc.t (toggles both on hardware, always restores osc_on_battery(1), leaves 32kHz off as the teardown power-save). Resolves F6 — ✅ 2026-07-16 attempt 1: PASS (dist t/80-power 8 tests; t/530 block board-gated, guarded for install lag). Reinstall rpi-rtc-ds3231 on the Pi to activate the t/530 guard.

## Archived Fixes

- Fix 1 (from V1): the plan assumed t/350-stepper.t de-energized the motor at exit via `cleanup()`, but its `$sm->cleanup` calls live only inside helper subs — the main teardown (207-211) did `$exp->cleanup; $pi->cleanup` and never de-energized `$sm`. Added a guarded `$sm->off if $sm->can('off')` before `$exp->cleanup` so the motor is explicitly parked at exit (the `can` guard covers the pre-3.1802 install lag; `$exp->cleanup` releases the pins on older installs).
