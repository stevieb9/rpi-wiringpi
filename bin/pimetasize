#!/usr/bin/env perl

use warnings;
use strict;
use feature 'say';

use Devel::Size qw(total_size);
use IPC::Shareable;

tie my %shared_pi_info, 'IPC::Shareable', {
    key => 'rpiw',
    create => 0,
};

while(1){
    say(total_size(\%shared_pi_info));
    sleep 2;
}

